# 实验 8：两级查表优化（float 子表 + 行并行，无 atomic）

## 1. 核心思路

### 1.1 问题

实验 6 的多核分组方案虽然将 BF16 子表塞入 L1，但 `#pragma omp atomic` 的缓存行竞争和受限并行度（G 核 vs 80 核）抵消了 L1 收益。实验 7 证实纯 L1 查表仅 ~3-4 周期，实验 6 的瓶颈在 atomic 而非查表本身。

### 1.2 方案：两级查表 + 行并行

去掉 BF16 压缩和 atomic，改用 **float 子表 + 行并行**：

```
实验6：BF16 子表 + G 核 atomic 合并  →  L1 命中但 atomic 瓶颈
实验8：float 子表 + 行并行 + 两级查表  →  L1 命中，无 atomic，满核并行
```

两级查表：
- **粗表**：根据 idxA 所属组选择对应的 float 子表指针（G 个条目，< 1 KiB）
- **细表**：每组 idxA 范围 × 256 的 float 值（4B/entry）

行并行：80 核各自处理不同的输出行 (i)，无需 atomic。

### 1.3 与实验 6 的关键区别

| 维度 | 实验 6（分组） | 实验 8（两级查表） |
|------|--------------|-----------------|
| 子表格式 | BF16（2B） | float（4B） |
| 子表大小 | 影响 L1 驻留 | 影响 L1 驻留 |
| BF16→float 转换 | 需要（左移 16 位） | **不需要** |
| 并行方式 | G 核同算一个 (i,j) | **行并行（满核）** |
| 核间同步 | `#pragma omp atomic` | **无** |
| 查表方式 | 标量加载子表 | **SVE gather** 从子表加载 |

### 1.4 子表大小

| G | step | 细表（float） | L1 驻留 | 行并行核数 |
|---|------|-------------|---------|-----------|
| 1 | 256 | 256×256×4B = 256 KiB | L2 | 80 |
| 2 | 128 | 128×256×4B = 128 KiB | L2 | 80 |
| 4 | 64 | 64×256×4B = 64 KiB | L1 边界 | 80 |
| 8 | 32 | 32×256×4B = 32 KiB | **L1** | 80 |
| 16 | 16 | 16×256×4B = 16 KiB | **L1** | 80 |
| 32 | 8 | 8×256×4B = 8 KiB | **L1** | 80 |
| 64 | 4 | 4×256×4B = 4 KiB | **L1** | 80 |

G≥4 时每个细表 ≤64 KiB，可 L1 驻留。

---

## 2. 数据流

### 2.1 预处理

与实验 6 相同的分组预处理，复用 `GroupData` / `preprocess_groups()`：

```
对每行 A[i]，按 idxA 值域分组：
  g = A[i][k] / (256/G)
  → A_grouped[]: 组内连续存储的 idxA
  → k_indices[]: 对应原始 k 位置（用于回追 B_T）
  → row_start/row_count: 每行每组的起始和计数
```

构建 G 个 float 子表：

```
for g in 0..G-1:
    fine_table[g] = full_lut[g*step .. (g+1)*step]  // float, 直接复制
```

### 2.2 运行时（行并行 + 两级查表）

```
#pragma omp parallel for schedule(static)    ← 80 核行并行
for i in 0..N-1:
    for j in 0..S-1:
        sum = 0
        for g in 0..G-1:                     ← 两级查表
            cnt = row_count[i*G+g]
            if cnt == 0: continue
            a_ptr = A_grouped[start]
            k_ptr = k_indices[start]
            fine  = fine_tables[g]            ← 粗表 → 细表

            阶段1（标量）：收集 B_T + 计算子表索引
                b_val = B_T[j][k_ptr[t]]
                idx[t] = (a_ptr[t] - base) * 256 + b_val

            阶段2（SVE）：gather + 累加
                acc = svadd_f32_m(acc, svld1_gather(子表, idx))
                sum += svaddv_f32(acc)

        C[i][j] = sum                          ← 无 atomic!
```

核心理解：**最内层循环按组处理**。处理组 g 时，只反复访问细表 g。细表 g 很小（G≥8 时 ≤32 KiB），在 L1 中保持热度，所有 gather 几乎全部 L1 命中。

---

## 3. 核函数：`lookup_two_level_omp`

### 3.1 源码

```cpp
void lookup_two_level_omp(const GroupData &data,
                           const std::vector<std::vector<float>> &fine_tables,
                           const uint8_t *B_T, float *C,
                           int N, int S, int L) {
    int G = data.G, step = data.step;
    #pragma omp parallel for schedule(static)
    for (int i = 0; i < N; ++i) {
        for (int j = 0; j < S; ++j) {
            float sum = 0.0f;
            for (int g = 0; g < G; ++g) {
                int cnt = data.row_count[i * G + g];
                if (cnt == 0) continue;
                int start = data.row_start[i * G + g];
                const uint8_t *a_ptr = data.A_grouped.data() + start;
                const int *k_ptr = data.k_indices.data() + start;
                const float *fine = fine_tables[g].data();
                int base = g * step;

                // 阶段1：标量收集 B_T + 计算子表索引
                alignas(64) uint32_t idx_buf[512];
                const uint8_t *b_row = B_T + j * L;
                for (int t = 0; t < cnt; ++t)
                    idx_buf[t] = (uint32_t)(a_ptr[t] - base) * 256u + b_row[k_ptr[t]];

                // 阶段2：SVE gather + 累加 + 归约
                svfloat32_t acc = svdup_n_f32(0.0f);
                int t = 0;
                svbool_t pg = svwhilelt_b32(t, cnt);
                while (svptest_any(svptrue_b32(), pg)) {
                    acc = svadd_f32_m(pg, acc,
                        svld1_gather_u32index_f32(pg, fine, svld1_u32(pg, idx_buf + t)));
                    t += svcntw();
                    pg = svwhilelt_b32(t, cnt);
                }
                sum += svaddv_f32(svptrue_b32(), acc);
            }
            C[i * S + j] = sum;
        }
    }
}
```

### 3.2 关键实现细节

**为什么用 `svld1_gather_u32index_f32` 而非标量加载？**

SVE gather 指令可以根据计算好的 32 位索引数组，从 float 子表的任意位置加载 8 个元素到向量寄存器。每个元素对应不同的索引，一次 gather 代替 8 次标量加载 + 插入。

**`svadd_f32_m` vs `svadd_f32_z`：**

与实验 6/7 相同，使用 merge 谓词 `_m` 确保尾部不活跃通道保留原值。

**`idx_buf` 计算免去 BF16→float 转换：**

子表是 float（4B），gather 直接加载 float，无需 BF16 的 `<< 16` 左移转换。索引计算仅比实验 6 多一次 `a_ptr[t] - base` 减法。

### 3.3 与实验 6 的对比

| 方面 | 实验 6 lookup_sve_grouped_omp | 实验 8 lookup_two_level_omp |
|------|-----------------------------|---------------------------|
| 并行方式 | G 核同算一个 (i,j)，所有核循环范围相同 | 行并行，每核算不同的 i 行 |
| 子表加载 | 标量 `sub[idx]` + `<< 16` | SVE gather `svld1_gather_u32index_f32` |
| 结果合并 | `#pragma omp atomic` 累加 | **直接写 C[i][j]** |
| 组间循环 | 核 g 只处理固定组 g | 每核遍历 **所有 G 组** |
| B_T 收集 | 标量通过 k_indices | 标量通过 k_indices（相同） |

---

## 4. 实验设置

| 参数 | 值 |
|------|-----|
| 矩阵 | N=128, S=328, L=512 |
| 子表格式 | float（4B/entry，直接 LUT 复制） |
| G 扫描 | 1, 2, 4, 8, 16, 32, 64 |
| 线程 | 80（行并行，`close` 绑定） |
| 预热 | 100 次 |
| 采样 | 10000 次 |
| 编译器 | `g++ -O3 -fopenmp -march=armv8.2-a+sve` |
| 平台 | HiSilicon Kunpeng, 256-bit SVE, L1d=64KiB |

### 4.1 预期分析

**G 越小 → 细表越大 → L1 miss 越多 → 查表延迟高**

| G | 细表 | 预期 L1-miss | 预期每次查表 | 说明 |
|---|------|------------|------------|------|
| 1 | 256 KiB | ~50-80% | ~12 cyc | 主要 L2 加载 |
| 2 | 128 KiB | ~30-50% | ~8-10 cyc | 部分 L1 |
| 4 | 64 KiB | ~5-15% | ~5-7 cyc | L1 边界 |
| 8 | 32 KiB | ~0-2% | ~4-5 cyc | L1 命中 |
| 16 | 16 KiB | ~0% | ~4 cyc | L1 全命中 |
| 32 | 8 KiB | ~0% | ~4 cyc | L1 全命中 |
| 64 | 4 KiB | ~0% | ~4 cyc | L1 全命中 |

**与实验 6 的对比：**

G=8 起，实验 8 应显著优于实验 6，因为：
1. 无 atomic 竞争（实验 6 中 G≥8 时 SVE 相位占比 >57%，atomic 主导）
2. 满 80 核并行（实验 6 最多 64 核，且核数 = G）
3. 无 BF16→float 转换开销

---

## 5. 输出格式

```
方案      线程  子表     级别  总耗时(μs)  GOP/s    vs基线  正确
------------------------------------------------------------------
基线SVE   80    256 KiB  L2    xxx.x      xx.xx   —       OK
TL-G=1    80    256 KiB  L2    xxx.x      xx.xx   x%      OK
TL-G=2    80    128 KiB  L2    xxx.x      xx.xx   x%      OK
TL-G=4    80    64 KiB   L1    xxx.x      xx.xx   x%      OK
TL-G=8    80    32 KiB   L1    xxx.x      xx.xx   x%      OK
TL-G=16   80    16 KiB   L1    xxx.x      xx.xx   x%      OK
TL-G=32   80    8 KiB    L1    xxx.x      xx.xx   x%      OK
TL-G=64   80    4 KiB    L1    xxx.x      xx.xx   x%      OK
```

---

## 6. 代码索引

| 函数 | 说明 |
|------|------|
| `build_float_fine_tables()` | 从完整 LUT 构建 G 个 float 子表 |
| `lookup_two_level_omp()` | 行并行 + 两级查表核函数 |
| `exp8_two_level_lookup()` | 主函数：G 扫描 + 基线对比 + 输出 |

### 编译运行

```bash
g++ -O3 -fopenmp -march=armv8.2-a+sve -o fp8_server \
    matmul_fp8_server.cpp -std=c++17 -lnuma

# 只运行实验 8
./fp8_server 8
```
