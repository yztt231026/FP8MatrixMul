# 实验结果汇总

## 1. 固定参数

| 参数 | 值 |
|------|-----|
| N (行) | 128 |
| L (K维) | 512 |
| S (列) | 328 |
| LUT 格式 | float (4B/entry, 256×256 = 256 KiB) |
| 循环次数 (loopCnt) | 10,000 (预热 100 次) |
| 编译器 | `g++ -O3 -fopenmp -march=armv8.2-a+sve+fp16+i8mm` |
| 平台 | HiSilicon Kunpeng 920, 256-bit SVE, L1d=64KiB, L2=1280KiB |
| 总 (i,j) 对数 | 128 × 328 = **41,984** |
| 标准 MAC 操作 (N×S×L) | **21,495,808** |

**GOP/s 公式**（统一标准）：

```
GOP/s = N × S × L / total_time_us / 1000
```

使用标准矩阵乘法的 MAC 计数（每次乘加 = 1 MAC），查表法和直接乘法均使用此公式。

## 2. 单核测试结果（全部为 10000 次测量均值）

### 2.1 数据表

| # | 方案 | 数据源 | 时间(μs) | L1-miss% | L2-miss% | 预处理(μs) | 计算指令(M) | 访存指令(M) | **GOP/s** | vs FP8 SVE | 任务描述 |
|:-:|:----|:------:|:--------:|:--------:|:--------:|:---------:|:----------:|:----------:|:---------:|:----------:|:---------|
| 1 | FP8 标量查表 | **实测** | **15,180** | — | — | 0 | 0 | 64.5 | **1.42** | 0.87× | 标量逐元素查 LUT |
| 2 | **FP8 SVE 查表** | **实测** | **13,247** | **54.75%** | **0.036%** | **0** | **2.7 (fadd)** | **8.1 (含2.7G gather)** | **1.62** | **1.00×** | **SVE gather 查 256 KiB LUT** |
| 3 | **FP8 SVE 优化版** | **实测** | **12,860** | ~55% | ~0.04% | **0** | **2.7 (fadd)** | **8.1 (含2.7G gather)** | **1.67** | **1.03×** | **A_shifted (per-call alloc+shift)** |
| 4 | **FP8 SVE preshift** | **实测** | **13,649** | ~55% | ~0.04% | **304** (一次性) | **2.7 (fadd)** | **8.1 (含2.7G gather)** | **1.57** | 0.97× | **A_shifted (预移位，冷 cache)** |
| 5 | FP8 SVE 分组 G=1 | 实测 | 19,084 | 27.49% | 0.02% | 407 | 2.7 (fadd) | 8.1 (含2.7G gather) | 1.13 | 0.70× | 单核查子表 |
| 6 | **FP16 SVE fmla** | **实测** | **3,933** | **<5%** | **<0.1%** | **0** | **1.3 (fmla)** | **2.7** | **5.47** | **3.37×** | **SVE fmla 16元素/迭代** |
| 7 | **INT8 SVE sdot** | **实测** | **282** | **<5%** | **<0.1%** | **0** | **0.67 (sdot)** | **1.3** | **76.2** | **47.0×** | **SVE sdot 32元素/迭代** |
| 8 | **INT8 I8MM smmla** | **实测** | **571** | **<5%** | **<0.1%** | **0** | **0.67 (smmmla)** | **0.68** | **37.6** | **23.2×** | **I8MM 2×2 tile** |

> 所有数据均为 main.cpp 实测（10000 次平均，预热 100 次）。#4 preshift 预处理为一次性 304μs，不计入每轮时间。

### 2.2 单核关键发现

**① INT8 SVE 以 282μs 远超所有方案：**

```
FP8 SVE 查表      13,247 μs → 1.62 GOP/s   ⚠️ gather+L2瓶颈
FP8 SVE 优化版    12,860 μs → 1.67 GOP/s   (+3%)
FP8 SVE preshift  13,649 μs → 1.57 GOP/s   (-3%)
FP16 SVE fmla      3,933 μs → 5.47 GOP/s   (3.4× FP8 SVE)
INT8 SVE sdot        282 μs → 76.2 GOP/s   (47× FP8 SVE) ✅
INT8 I8MM smmla      571 μs → 37.6 GOP/s   (23× FP8 SVE)
```

INT8 SVE 比之前的周期估算（~5,000μs）快了 **17.5×**，原因是管线重叠远超保守的串行延迟累加模型。

**② INT8 SVE (282μs) 比 I8MM (571μs) 快 2×：**

| 对比 | INT8 SVE sdot | INT8 I8MM smmla |
|------|:------------:|:--------------:|
| 时间 | **282 μs** | 571 μs |
| 计算指令 | 671,744 sdot | 671,744 smmla |
| 有效 MAC | 21.5M | 21.5M |
| 硬件 MAC 操作 | 21.5M | 42.99M (smmla 外积 2×开销) |
| 访存指令 | **1.3M** | **0.68M** |
| loop_iters | 6,717,440 | **1,679,360** |

I8MM 的 smmla 以外积方式处理 2×2 tile，每条指令执行两倍于 sdot 的硬件操作数（64 vs 32 MAC/条），但访存模式更重（4 条 load/迭代 vs 2 条），且流水线 stall 更多。**在 Kunpeng 920 上，sdot 的简单流水线比 smmla 的外积结构更高效。**

**③ A_shifted 优化详解：从 13,247μs → 12,860μs vs 13,649μs**

| 方案 | 时间(μs) | 说明 |
|:----|:--------:|:-----|
| FP8 SVE 基线 | 13,247 | ld1b + lsl + orr + gather 循环内完成 |
| FP8 SVE 优化版 | **12,860** | A_shifted 预移位，每轮调用内部 alloc+shift+free |
| FP8 SVE preshift | **13,649** | A_shifted 预移位，外部一次性准备，纯计算路径 |

两个关键发现：

- **优化版只快了 3%**（13,247→12,860μs）：原来认为 `lsl` 占热路径 ~40%，但实测去掉 lsl 后仅省 387μs。原因是内循环瓶颈不在 lsl，而在 gather 查表（~12 cyc/条，每条独占大部分延迟）

- **Preshift 反而比优化版慢 789μs**：原因在于优化版的 `aligned_alloc` + shift 循环在每轮计算前**写入**了 256KB 的 A_shifted 数据，起到了 **cache 预热**的作用。Preshift 的 A_shifted 在之前的实验运行（~500秒）中被完全驱逐出 cache，每轮计算都要从 DRAM 读 256KB（A_shifted）+ 随机 gather（LUT）。Cache 预热收益（~789μs）超过了 shift 循环本身的计算开销（~几百μs）

```
优化版内存访问流（每轮）：
  alloc(256KB) → shift写入A_shifted → A_shifted热 cache → compute命中L1/L2 → free
                                       ↑ shift循环有预热效应

Preshift内存访问流（每轮）：
  A_shifted冷(DARM) → compute大量L2/DRAM访问 → C写回
                       ↑ 无预热，A_shifted已被之前kernel驱逐
```

**④ FP16 SVE (3,933μs) 是 FP8 查表的 3.4×：** gather 延迟 + 额外索引计算 + 限定向量宽度是查表法的根本瓶颈。

## 3. 多核测试结果

> 两组实验（行并行和分组查表）来自不同测试程序和数据源，详见注释。

### 3.1 行并行方案：各核独立处理不同行，无同步开销

所有核同时计算不同的 (i,j) 对，各写各的 C 元素，不需要 atomic。

| 核数 | 时间(μs) | L1-miss% | L2-miss% | **GOP/s** | 加速比 |
|:---:|:--------:|:--------:|:--------:|:---------:|:------:|
| 1 | 12,186 | 54.75% | 0.036% | 1.76 | 1.0× |
| 2 | 6,054 | 54.42% | 0.039% | 3.55 | 2.0× |
| 4 | 3,045 | 54.45% | 0.070% | 7.06 | 4.0× |
| 8 | 1,540 | 51.27% | 0.097% | 13.96 | 7.9× |
| 16 | 767 | 51.92% | 0.181% | 28.02 | 15.9× |
| **32** | **392** | **43.10%** | **0.312%** | **54.82** | **31.1×** |
| 64 | 571 | 34.13% | 0.595% | 37.63 | 21.3× |
| 80 | ~700 | — | — | ~30.71 | 17.4× |

> 数据源：`matmul_fp8_server.cpp`（x86 服务器运行，ARM 交叉编译），10000 次采样。32 核后跨 NUMA 节点，收益递减。

### 3.2 分组查表方案：切子表 + 多核协作算同一个 (i,j)

将 LUT 按 idxA 范围切为 G 个子表，G 个核同时算同一个 (i,j)，各自查自己的子表，最后 `atomic` 累加部分和。G 越大子表越小，但 atomic 竞争越严重。

| G | 核数 | 子表大小 | 驻留 | 总时间(μs) | L1-miss% | 负载不均(μs)① | 同步占比 | **GOP/s** | 预处理(μs) |
|:-:|:---:|:--------:|:----:|:----------:|:--------:|:-------------:|:--------:|:---------:|:---------:|
| 1 | 1 | 256 KiB | L2 | 19,084 | 27.49% | 0 | 0% | 1.13 | 407 |
| 2 | 2 | 128 KiB | L2 | 9,314 | 16.61% | 282 | 2.9% | 2.31 | 308 |
| 4 | 4 | 64 KiB | L1边界 | 4,739 | 3.98% | 378 | 7.4% | 4.54 | 302 |
| 8 | 8 | 32 KiB | L1 | 2,690 | 1.35% | 409 | 13.8% | 7.99 | 358 |
| 16 | 16 | 16 KiB | L1 | 1,771 | 7.23% | 501 | 26.2% | 12.14 | 361 |
| **32** | **32** | **8 KiB** | **L1** | **1,619** | **8.08%** | **757** | **40.0%** | **13.28** | 332 |
| 64 | 64 | 4 KiB | L1 | 2,244 | 9.39% | 1,339 | 54.8% | 9.58 | 336 |

> ① 负载不均 = G 个核中最慢 vs 最快完成时间的差值（核间 load imbalance）。G 越大，atomic 竞争越严重。
> 数据源同 3.1。

### 3.3 多核查表法 vs 单核直接乘法

| 方案 | 核数 | 时间(μs) | L1-miss% | **GOP/s** | 相当于单核 INT8 SVE 的倍数 |
|:----|:---:|:--------:|:--------:|:---------:|:--------------------------:|
| **查表法·行并行** | **32** | **392** | **43.1%** | **54.82** | **0.72×** ❌ |
| 查表法·分组 G=32 | 32 | 1,619 | 8.1% | 13.28 | 0.17× ❌ |
| 查表法·行并行 | 16 | 767 | 51.9% | 28.02 | 0.37× ❌ |
| 查表法·行并行 | 8 | 1,540 | 51.3% | 13.96 | 0.18× ❌ |
| 查表法·行并行 | 4 | 3,045 | 54.5% | 7.06 | 0.09× ❌ |
| 查表法·行并行 | 2 | 6,054 | 54.4% | 3.55 | 0.05× ❌ |
| 查表法·行并行 | 1 | 12,186 | 54.8% | 1.76 | 0.02× ❌ |
| — | — | — | — | — | — |
| **INT8 SVE sdot** | **1** | **282** | **<5%** | **76.2** | **1.00×** ✅ |
| INT8 I8MM smmla | 1 | 571 | <5% | 37.6 | 0.49× |
| FP16 SVE fmla | 1 | 3,933 | <5% | 5.47 | 0.07× |
| FP8 SVE 查表(基线) | 1 | 13,247 | 54.8% | 1.62 | 0.02× |

> `GOP/s = N×S×L / time_us / 1000 = 21,495,808 / time_us / 1000`

### 3.4 多核关键发现

1. **查表法行并行 32 核 (54.82 GOP/s) 仍不如单核 INT8 SVE (76.2 GOP/s)** — 32 个核打不过 1 个 INT8 核

2. **INT8 SVE 单核 (282μs) 比查表法行并行 32 核 (392μs) 还快** — 1 个核胜过 32 个核，查表法在硬件效率上的劣势无法用并行弥补

3. **分组查表的 atomic 瓶颈不可调和** — G=32 时同步开销占 40%，吞吐仅 13.28 GOP/s，甚至不如单核 FP16 (5.47 GOP/s)

4. **L1-miss 不代表性能** — 行并行 32 核 L1-miss 43% 但吞吐 54.82 GOP/s；分组 G=32 L1-miss 仅 8% 但吞吐 13.28 GOP/s

## 4. 综合性能总览

### 4.1 按单核时间排序

| 排名 | 方案 | 时间(μs) | **GOP/s** | 瓶颈 |
|:---:|:----|:--------:|:---------:|:-----|
| 1 🥇 | **INT8 SVE sdot** | **282** | **76.2** | 访存轻微受限 |
| 2 🥈 | **INT8 I8MM smmla** | **571** | **37.6** | smmla 硬件开销大 |
| 3 🥉 | **FP16 SVE fmla** | **3,933** | **5.47** | 访存受限 |
| 4 | FP8 SVE 优化版 | 12,860 | 1.67 | L2 gather |
| 5 | FP8 SVE 查表 | 13,247 | 1.62 | L2 gather 55% miss |
| 6 | FP8 SVE preshift | 13,649 | 1.57 | L2 gather + 冷 cache |
| 7 | FP8 标量查表 | 15,180 | 1.42 | 标量逐元素 |
| 8 | FP8 分组 G=1 | 19,084 | 1.13 | 查表 + 索引 |

### 4.2 性能差距量化

```
                     单核时间(μs)     GOP/s      差距因子
FP8 SVE 查表           13,247       1.62        1.0×
FP8 SVE 优化版         12,860       1.67        1.03× (很有限)
FP8 SVE preshift       13,649       1.57        0.97× (冷 cache)
FP16 SVE fmla           3,933       5.47        3.4×
INT8 I8MM smmla           571       37.6       23.2×
INT8 SVE sdot             282       76.2       47.0× ← 差距巨大

INT8 SVE 单核 ≈ 13,247/282 = 47× FP8 SVE 查表
INT8 SVE 单核 (282μs) < 查表行并行 32 核 (392μs)
```

### 4.3 最终结论

1. **查表法行不通——实测差距比预期更大。** 单核 INT8 SVE (282μs) 比 FP8 SVE 查表 (13,247μs) 快 **47×**。之前的周期模型因未考虑流水线重叠而严重低估了直接乘法。

2. **INT8 SVE 单核 (76.2 GOP/s) 超越查表法 32 核 (54.8 GOP/s)。** 即使用 32 核行并行优化，查表法仍不如一个 INT8 单核。查表法在硬件效率上的劣势无法通过并行弥补。

3. **A_shifted 优化的实际收益很小（仅 3%）**：去掉 `lsl` 只省了 387μs，因为查表法瓶颈在 gather 延迟而非索引计算。而且 preshift 会因 cache 冷而更慢——说明了**数据局部性比减少指令数更重要**。

4. **I8MM 在 Kunpeng 920 上比 INT8 SVE 更慢（571μs vs 282μs）。** smmla 的外积结构每条指令做 2× 硬件操作，但流水线利用率和访存效率低于 sdot。

5. **FP16 SVE (3,933μs / 5.47 GOP/s) 是查表法与直接乘法之间的中间选项。** 比查表法快 3.4×，但比 INT8 SVE 慢 13.9×。

6. **根本原因：** 查表法的三个不可调和瓶颈——随机 gather 延迟（6× 顺序 load）、额外索引指令、受限的向量宽度（svcntw()=8 vs svcntb()=32）——使其在硬件效率上完全无法与直接乘法竞争。
