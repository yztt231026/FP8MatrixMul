# FP8 查表 vs FP16/INT8 直接乘法：汇编指令对比分析

## 1. 分析目标

通过反汇编 `main.cpp` 中 7 个核函数，从指令层面解释：**为什么 FP8 查表法远慢于 FP16/INT8 直接矩阵乘法？**

实验测量显示，在相同矩阵尺寸（N=128, S=328, L=512）下，FP8 查表 SVE 版本的时延远高于 FP16/INT8 直接乘。本报告从汇编指令构成和内存访问模式两个维度分析根本原因。

---

## 2. 函数指令总览

### 2.1 各函数指令数量

编译选项：`-O3 -march=armv8.2-a+sve+fp16+i8mm`

| 函数 | 总指令数 | 标量指令 | SVE 指令 | 分支 | 每向量处理元素数 | 每元素指令成本 |
|------|:-------:|:--------:|:--------:|:---:|:--------------:|:------------:|
| `matmul_fp8_lookup_scalar` | 44 | 35 | 0 | 7 | 1 | **44.0** |
| `matmul_fp8_lookup_sve` | **168** | 120 | 11 | 19 | 8 | **21.0** |
| `matmul_fp8_lookup_sve_optimized` | **96** | 69 | 6 | 10 | 8 | **12.0** |
| `dotProductSVE` (FP16) | 25 | 9 | 5 | 4 | 8 | 3.1 |
| `matmul_sve_fp16` | **67** | 40 | 7 | 9 | 8 | **8.4** |
| `matmul_int8_sve` | **60** | 35 | 5 | 9 | 64 | **0.94** |
| `matmul_int8_i8mm_complete` | 191 | 130 | 19 | 16 | 256 | 0.75 |

### 2.2 指令大类分布

| 函数 | 总指令 | 标量 ALU | 标量 Load | 标量 Store | SVE Load | SVE Gather | SVE 向量 ALU | SVE 谓词 | SVE 归约 | 分支 |
|------|:-----:|:--------:|:---------:|:----------:|:--------:|:----------:|:----------:|:--------:|:--------:|:---:|
| FP8 标量查表 | 44 | 22 | 2 | 0 | 0 | 0 | 0 | 0 | 0 | 7 |
| FP8 SVE 查表 | 168 | 60 | 29 | 11 | 7 | 0 | 4 | 12 | 0 | 19 |
| FP8 SVE 优化 | 96 | 33 | 13 | 9 | 4 | 0 | 2 | 6 | 0 | 10 |
| FP16 SVE | 67 | 22 | 10 | 8 | 2 | 0 | 3 | 4 | 2 | 9 |
| INT8 SVE dot | 60 | 22 | 8 | 8 | 2 | 0 | 3 | 4 | 2 | 9 |
| INT8 I8MM | 191 | 66 | 23 | 17 | 9 | 0 | 10 | 12 | 10 | 16 |

> **注**：`svld1_gather_u32index_f32` 在 objdump 中显示为 `ld1w`，被归类为 "SVE Load"，但其延迟特性与顺序 `ld1w` 完全不同。

---

## 3. 核心原因分析

### 3.1 原因一：随机 gather vs 顺序 load（主因）

这是最根本的区别。

| 维度 | FP8 查表法 | FP16/INT8 直接乘法 |
|------|-----------|-------------------|
| 访存指令 | `svld1_gather_u32index_f32` | `svld1_f16` / `svld1_s8` |
| 访存模式 | **随机 gather**（地址 = table[indices[i]]） | **顺序 load**（连续递增地址） |
| 工作集 | 256 KiB（完整 LUT） | 1 KiB（A 一行 + B_T 一行） |
| L1 命中率 | ~50% L1-miss | **100% L1 hit** |
| 单指令延迟 | gather ~7-10 cyc（L2 ~12 cyc） | 顺序 ld1 ~1-2 cyc |
| 硬件预取 | ❌ 无法预取（地址随机） | ✅ 顺序预取有效 |

FP8 的每一次 `svld1_gather` 都是一次**随机内存访问**：索引值由 `(idxA << 8) | idxB` 决定，无法预测下一个地址。完整 LUT 256 KiB 远超 L1d 64 KiB 的限制，每次 gather 大概率 L1-miss，需要从 L2 加载（~12 cycles）。

而 FP16/INT8 的 `svld1_f16`/`svld1_s8` 是**顺序连续加载**：每向量 8 个元素地址连续，硬件预取器可提前将下一缓存行拉入 L1，延迟仅 1-2 cycles，且流水线可多发射隐藏延迟。

### 3.2 原因二：额外索引计算指令（次因）

FP8 查表需要将两个 uint8 索引拼接为线性偏移：

```
FP8 SVE 查表（每向量迭代）：
  ld1ub_u32  pg, [rowA+k]       ← 加载 8 个 uint8 并拓宽为 uint32
  ld1ub_u32  pg, [rowB+k]       ← 加载 8 个 uint8 并拓宽为 uint32
  lsl        z, idxA, #8         ← 左移 8 位（等价于 ×256）
  orr        z, idxA_sh, idxB    ← 拼接为线性索引
  ld1_gather pg, [table, indices] ← ⚠️ 随机 gather
  fadd       pg, acc, vals       ← 累加

  → 6 条 SVE 指令 / 8 元素

FP16 SVE 乘加（每向量迭代）：
  ld1_f16    pg, [rowA+k]       ← 加载 8 个 float16
  ld1_f16    pg, [rowB+k]       ← 加载 8 个 float16
  fmla       pg, acc, va, vb    ← 乘加融合（1 条指令完成乘法 + 加法）
  
  → 3 条 SVE 指令 / 8 元素

INT8 SVE 点积（每向量迭代）：
  ld1_s8     pg, [rowA+k]       ← 加载 64 个 int8
  ld1_s8     pg, [rowB+k]       ← 加载 64 个 int8
  sdot       acc, va, vb        ← 64 元素点积（1 条指令）

  → 3 条 SVE 指令 / 64 元素
```

FP8 查表每 8 元素需要 **6 条 SVE 指令**，其中 1 条是高延迟 gather。  
FP16 乘加每 8 元素仅需 **3 条指令**，全部是低延迟顺序操作。  
INT8 点积每 **64 元素**仅需 **3 条指令**，计算密度最高。

### 3.3 原因三：计算密度差异

| 指标 | FP8 查表 SVE | FP16 SVE 乘加 | INT8 SVE dot | INT8 I8MM |
|------|:----------:|:------------:|:-----------:|:--------:|
| 每向量元素数 | 8 | 8 | **64** | **256** |
| 每元素 SVE 指令数 | 0.75 | 0.38 | **0.047** | **0.074** |
| 每元素标量指令数 | 7.5 | 2.75 | 0.55 | 0.51 |
| 瓶颈类型 | **访存（L2 gather）** | 计算/访存平衡 | **计算（sdot）** | **计算（smmmla）** |
| 是否受 cache 容量限制 | ✅ 是（256 KiB） | ❌ 否（1 KiB） | ❌ 否（1 KiB） | ❌ 否（1 KiB） |

FP8 查表每元素需要 0.75 条 SVE 指令 + 7.5 条标量指令（循环控制、地址计算等），且每条 gather 指令都是高延迟的随机访存。FP16 的每元素指令成本已显著较低，INT8 的 `svdot` 和 `smmmla` 更是将计算密度提升了 1~2 个数量级。

### 3.4 汇编热路径逐条对比

#### FP8 查表 SVE（`matmul_fp8_lookup_sve`）

热路径位于最内层 SVE gather + 累加循环（每 (i,j) 执行 512/8 = 64 次）：

```
地址：           指令                      操作数             延迟估算
------------------------------------------------------------------------
[loop_start:]
whilelt        p0.s, Xk, XL              ← 谓词生成        1 cyc
ld1ub_u32      Z0.s, P0/Z, [XrowA, Xk]  ← 加载 idxA        2 cyc
ld1ub_u32      Z1.s, P0/Z, [XrowB, Xk]  ← 加载 idxB        2 cyc
lsl            Z0.s, P0/M, Z0.s, #8     ← idxA << 8        1 cyc
orr            Z1.s, P0/M, Z0.s, Z1.s   ← idxA | idxB      1 cyc
LD1W (gather)  Z2.s, P0/Z, [Xtable, Z1.s, UXTW #2]  ← ⚠️ RANDOM L2   ~12 cyc
fadd           Z3.s, P0/M, Z3.s, Z2.s   ← acc += vals      2 cyc
add            Xk, Xk, #8               ← k += svcntw()    1 cyc
ptest          p0.s, P7, p0.s           ← 检查谓词          1 cyc
b.any          [loop_start]             ← 循环分支          0-2 cyc
------------------------------------------------------------------------
每迭代总延迟：     ~24 cyc / 8 元素 = ~3 cyc/元素
```

其中 **gather 占 ~12 cyc = 50% 的迭代时间**，是明确的瓶颈。

#### FP16 SVE 乘加（`matmul_sve_fp16`）

```
[loop_start:]
whilelt        p0.h, Xk, XL              ← 谓词生成        1 cyc
ld1_f16        Z0.h, P0/Z, [XrowA, Xk]  ← 加载 va          2 cyc
ld1_f16        Z1.h, P0/Z, [XrowB, Xk]  ← 加载 vb          2 cyc
fmla           Z2.h, P0/M, Z0.h, Z1.h   ← acc += va * vb   3 cyc
add            Xk, Xk, #16              ← k += svcnth()    1 cyc
ptest          p0.h, P7, p0.h            ← 检查谓词          1 cyc
b.any          [loop_start]             ← 循环分支          0-2 cyc
------------------------------------------------------------------------
每迭代总延迟：     ~10 cyc / 8 元素 = ~1.25 cyc/元素
```

**无 cache miss，fmla 可流水，瓶颈在计算而非访存。**

#### INT8 SVE 点积（`matmul_int8_sve`）

```
[loop_start:]
whilelt        p0.b, Xk, XL              ← 谓词生成        1 cyc
ld1_s8         Z0.b, P0/Z, [XrowA, Xk]  ← 加载 va          2 cyc
ld1_s8         Z1.b, P0/Z, [XrowB, Xk]  ← 加载 vb          2 cyc
sdot           Z2.s, Z0.b, Z1.b          ← 64 元素点积       3 cyc
add            Xk, Xk, #64              ← k += svcntb()    1 cyc
ptest          p0.b, P7, p0.b            ← 检查谓词          1 cyc
b.any          [loop_start]             ← 循环分支          0-2 cyc
------------------------------------------------------------------------
每迭代总延迟：     ~10 cyc / 64 元素 = ~0.16 cyc/元素
```

**每向量处理 64 元素，计算密度是 FP8 的 ~19 倍。**

---

## 4. 量化总结

### 4.1 每 (i,j) 输出元素的总指令成本

假设 N=128, S=328, L=512，每 (i,j) 处理 512 个 k：

| 版本 | 标量指令 | SVE 指令 | gather 次数 | 估算总 cyc | 相对 FP16 |
|------|:-------:|:--------:|:----------:|:---------:|:--------:|
| FP8 标量 | 22,528 | 0 | 0 | ~45,000 | **6.7×** |
| FP8 SVE 优化 | 594 | 48 | 64/gather | ~24,000 | **3.6×** |
| FP16 SVE | 290 | 40 | 0 | ~6,700 | **1.0×** |
| INT8 SVE dot | 180 | 30 | 0 | ~2,700 | **0.4×** |
| INT8 I8MM | 680 | 100 | 0 | ~1,700 | **0.25×** |

> 估算基于：标量指令 ~2 cyc/条，顺序 SVE ~3 cyc/条，gather ~12 cyc/次。真实值因流水线并行和 cache 状态会有差异。

### 4.2 瓶颈公式

```
FP8 查表总时间 ≈  N × S × (L/8) × (t_gather + t_index + t_accum + t_loop)
                ↑     ↑     ↑       ↑
              128   328    64    gather 占 ~50%

FP16 总时间     ≈  N × S × (L/8) × (t_load + t_fmla + t_loop)
                ↑     ↑     ↑       ↑
              128   328    64    无 cache miss, 流水线友好

INT8 总时间     ≈  N × S × (L/64) × (t_load + t_dot + t_loop)
                ↑     ↑     ↑
              128   328    8    每迭代 64 元素, 迭代次数仅 FP8 的 1/8
```

### 4.3 结论

FP8 查表法比 FP16/INT8 直接乘法慢，**不是查表算法本身的问题，而是它的访存模式决定了它无法充分利用现代 CPU 的缓存层次**：

1. **随机 gather 是根本瓶颈**：每次查表都是随机地址访问，L1-miss 率 ~50%，依赖 L2 延迟（~12 cyc），且无法预取
2. **工作集过大**：完整 LUT 256 KiB 无法放入 L1d 64 KiB，FP16/INT8 的工作集仅 1 KiB
3. **计算密度低**：每 8 元素需要 1 条高延迟 gather + 多条索引计算指令，而 INT8 `svdot` 每 64 元素只需 1 条低延迟指令

**优化的方向不是消除查表，而是让查表变成 L1 hit**——这正是实验 8（两级查表）的核心思路：将大 LUT 拆分为 L1 可容纳的子表，让 SVE gather 从 L2 延迟变成 L1 延迟，同时保留行并行避免 atomic 竞争。
