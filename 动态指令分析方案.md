# main.cpp 运行时动态指令数分析方案

## 1. 分析目标

对 main.cpp 中 5 个核函数，测量运行时动态指令数并按功能分类：

| 指令类别 | FP8 查表 | FP16 乘加 | INT8 dot | INT8 I8MM |
|---------|:-------:|:---------:|:--------:|:---------:|
| ① 加载 LUT/查表 | ✅ gather | ❌ | ❌ | ❌ |
| ② 加载 A 矩阵元素 | ✅ | ✅ | ✅ | ✅ |
| ③ 加载 B 矩阵元素 | ✅ | ✅ | ✅ | ✅ |
| ④ 索引计算 (shift+or) | ✅ | ❌ | ❌ | ❌ |
| ⑤ 乘法/点积运算 | ❌ | ✅ fmla | ✅ sdot | ✅ smmla |
| ⑥ 加法/累加 | ✅ fadd | 含在 fmla | 含在 sdot | 含在 smmla |
| ⑦ 归约 (faddv/saddv) | ✅ | ✅ | ✅ | ✅ |
| ⑧ 循环控制 (whilelt/ptest/b) | ✅ | ✅ | ✅ | ✅ |
| ⑨ 函数框架 (prologue/epilogue) | ✅ | ✅ | ✅ | ✅ |

---

## 2. 测量工具

### 2.1 `perf stat` —— 总指令数 + Cycles

```bash
# 每 kernel 单独跑，测总指令数
perf stat -e instructions:u,cycles:u,LLC-loads:u,LLC-load-misses:u \
  ./main 2>&1 | grep -E "instructions|cycles|LLC"
```

输出示例：
```
    5,234,567,890  instructions:u
   12,345,678,901  cycles:u
```

### 2.2 `perf annotate` —— 指令级热点计数

```bash
perf record -e instructions:u ./main
perf annotate --stdio > /tmp/perf_annotate.txt
```

`perf annotate` 会给出每个函数的每条汇编指令的**采样计数**，例如：

```
Percent |  Source code & Assembly
---------------------------------
  5.23  │  whilelt p0.s, x0, x1
  2.15  │  ld1b    z0.s, p0/z, [x2]
  3.42  │  ld1b    z1.s, p0/z, [x3]
  8.91  │  ld1w    z2.s, p0/z, [x4, z0.s, uxtw #2]   ← gather 指令
  4.23  │  fadd    z3.s, p0/m, z3.s, z2.s
```

根据采样比例 × 总指令数，即可推算每条指令的动态执行次数。

### 2.3 PMU 事件（鲲鹏特定）

若 `perf` 支持，可用更多 ARM PMU 事件细分指令类型：

```bash
# ARM PMU 事件（需 root 或用 /sys/bus/event_source/devices/armv8_pmuv3/events/）
perf stat -e \
  instructions:u,              # 总指令数
  armv8_pmuv3/inst_retired/,
  armv8_pmuv3/ld_retired/,    # load 指令数
  armv8_pmuv3/st_retired/,    # store 指令数
  armv8_pmuv3/sve_inst_retired/, # SVE 指令数（如支持）
  armv8_pmuv3/mem_access/     # 内存访问指令数
  ./main
```

> 注意：鲲鹏 920 的 PMU 事件名称可能因内核版本而异，需要先 `ls /sys/bus/event_source/devices/armv8_pmuv3/events/` 确认。

---

## 3. 指令分类方法

### 3.1 方案 A：基于静态分析 × 循环次数推算（无需新测量）

已有 8 个函数的**静态指令数**（来自上一轮 objdump 分析）。结合每函数的**循环迭代次数**和**调用次数**，即可推算动态指令数：

```python
# 每个 (i,j) 对：内循环迭代次数 = L / 向量宽度
# 总调用次数 = N × S = 128 × 328 = 41,984

# 示例：FP8 SVE 查表
iter_per_ij = 512 / 8  # 64 次 SVE 迭代
ij_pairs = 128 * 328   # 41,984

# 热路径指令数（不含函数框架）
hot_insts_per_iter = {
    'load_A':        2,    # ld1ub_u32 × 2
    'index_compute': 2,    # lsl + orr
    'gather_lut':    1,    # ld1_gather  ← 关键
    'accumulate':    1,    # fadd
    'loop_control':  3,    # whilelt + ptest + b  
}

total_dynamic = sum(hot_insts_per_iter.values()) * iter_per_ij * ij_pairs
# = 9 × 64 × 41,984 ≈ 24.2M 条动态指令（仅热路径）
```

### 3.2 方案 B：`perf annotate` 采样（推荐，更准确）

```bash
# 为每个 kernel 单独测量
# 先修改 main.cpp，用命令行参数选择跑哪个 kernel

# 编译（加 -g 以便 annotate 能对应源码）
g++ -O3 -g -march=armv8.2-a+sve+fp16+i8mm \
    main.cpp -std=c++17 -o /tmp/main_prof

# 采样指令事件
perf record -e instructions:u -c 1000 /tmp/main_prof

# 生成带指令计数标注的汇编
perf annotate --stdio > /tmp/annotate_output.txt
```

`perf annotate` 的输出示例：

```
matmul_fp8_lookup_sve:
       │  whilelt p0.s, x0, x1                    ▒
  2.1% │  ld1ub   z0.s, p0/z, [x2, x0]            ← 加载 A[k]
  2.0% │  ld1ub   z1.s, p0/z, [x3, x0]            ← 加载 B[k]
  1.9% │  lsl     z0.s, p0/m, z0.s, #8            ← 索引计算
  1.8% │  orr     z1.s, p0/m, z0.s, z1.s          ← 索引计算
  8.5% │  ld1w    z2.s, p0/z, [x4, z1.s, uxtw #2] ← 查表 gather
  3.9% │  fadd    z3.s, p0/m, z3.s, z2.s          ← 累加
       │  add     x0, x0, #8                       ← k += 8
       │  ptest   p0.s, p7, p0.s                   ← 循环测试
  2.3% │  b.any   <loop>                           ← 分支
```

**各指令类别的占比 = 该类指令采样百分比之和**。乘以总指令数即得动态计数。

### 3.3 方案 C：代码插桩（最精确但需改代码）

在 main.cpp 中手动插桩计数：

```cpp
// 全局计数器
uint64_t g_load_A = 0, g_load_B = 0, g_gather_lut = 0;
uint64_t g_index_calc = 0, g_accum = 0, g_loop_ctrl = 0;

// 在热路径中累加
while (svptest_any(svptrue_b32(), pg)) {
    g_load_A += svcntw();      // ld1ub_u32 处理 svcntw() 个元素
    g_load_B += svcntw();
    g_index_calc += svcntw();  // lsl + orr 各一次
    
    svuint32_t idxA = svld1ub_u32(pg, &rowA[k]);
    svuint32_t idxB = svld1ub_u32(pg, &rowB[k]);
    svuint32_t indices = svorr_u32_z(pg, svlsl_n_u32_z(pg, idxA, 8), idxB);
    
    g_gather_lut += svcntw();  // 1 条 gather
    svfloat32_t vals = svld1_gather_u32index_f32(pg, table, indices);
    
    g_accum += svcntw();       // 1 条 fadd
    acc_v = svadd_f32_z(pg, acc_v, vals);
    
    g_loop_ctrl += 2;          // whilelt + ptest
    k += svcntw();
    pg = svwhilelt_b32(k, L);
}
```

输出各计数器的值，即可精确知道每种操作的执行次数。

---

## 4. 推荐流程

| 步骤 | 方法 | 产出 |
|------|------|------|
| 1 | `perf stat -e instructions:u` 各 kernel | 总动态指令数 |
| 2 | `perf annotate` 各 kernel | 指令级比例 |
| 3 | 静态分析 × 循环次数推算 | 理论估算 |
| 4 | 三组数据交叉验证 | 最终分类数据 |

输出格式示例：

```
Kernel: matmul_fp8_lookup_sve
总动态指令数: 1,234,567,890

指令类别         动态数量        占比     静态指令数/迭代
──────────────────────────────────────────────────────
加载 A 矩阵       123,456,789   10.0%    2 (ld1ub_u32)
加载 B 矩阵       123,456,789   10.0%    2 (ld1ub_u32)
索引计算          123,456,789   10.0%    2 (lsl + orr)
查表 gather       61,728,395    5.0%     1 (ld1_gather)
累加 fadd         61,728,395    5.0%     1 (fadd)
循环控制          185,185,185   15.0%    3 (whilelt+ptest+b)
归约              41,984        0.003%   1 (faddv/输出元素)
函数框架          ~500,000      0.04%    —
```

---

## 5. 预期结论

通过动态指令分解可以回答的问题：

1. **FP8 查表到底每条指令花了多少比例？**
   - gather 指令数虽少（静态 1 条），但因循环次数多，动态占比可观
   - gather 的延迟（~12 cyc）比指令数更关键

2. **FP16 fmla 真的占主导吗？**
   - fmla 动态指令数 ≈ 加载 A/B 之和
   - 因 fmla 可流水，实际 Cycles 占比低于指令数占比

3. **INT8 sdot 的计算密度优势有多大？**
   - 处理 64 元素/次，循环次数 = 512/64 = 8 次/(i,j)
   - 对比 FP8 查表循环 64 次/(i,j)，循环控制指令节省 ~8×
