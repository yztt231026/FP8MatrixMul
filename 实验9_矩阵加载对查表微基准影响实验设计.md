# 实验 9：矩阵加载对查表微基准的影响

## 1. 背景

实验 7 的微基准中，idxA/idxB 来自预生成的小数组 `a_vals` / `b_vals`（各 128 字节），它们在 L1 中始终命中，**不反映真实矩阵乘法中加载 A[i][k] 和 B_T[j][k] 的内存开销**。

实验 9 在 exp7 的三阶段计时框架下，将预生成数组替换为**完整的 A 矩阵（128×512）和 B_T 矩阵（328×512）**，在真实矩阵规模下测量矩阵加载对查表延迟和相位分解的影响。

## 2. 核心变化

| 环节 | exp7（纯查表） | exp9（矩阵加载） |
|------|-------------|----------------|
| idxA 来源 | `a_vals[i]`（128B 小数组，L1） | `A[i][k]`（512B A 矩阵行，L1 复用 328 次） |
| idxB 来源 | `b_vals[i]`（128B 小数组，L1） | `B_T[j][k]`（512B B_T 矩阵行，**L2 加载**） |
| 索引计算 | `a * TABLE_B + b` | `(A[i][k] % table_a) * 256 + B_T[j][k]` |
| 矩阵数据 | 无 | A: 128×512 = 64 KB，B_T: 328×512 = 164 KB |
| 查表次数/迭代 | 128~8（随 table_a 变化） | **N×S×L = 21,495,808**（固定） |
| 采样次数 | 20000 | 100~200（单次迭代已含大量工作） |
| 三阶段计时 | 相同 | 相同 |

## 3. 数据结构与 Cache 分析

### 3.1 矩阵规模

| 数据 | 维度 | 大小 | Cache 层级 |
|------|------|------|-----------|
| A | 128×512 uint8_t | 64 KB | L1d 边界（64 KiB） |
| B_T | 328×512 uint8_t | 164 KB | L2（1.25 MiB/核） |
| BF16 子表 | table_a × 256 × 2B | 1~16 KiB | L1（≤16 KiB ≪ 64 KiB） |
| C | 128×328 float | 164 KB | L2 |

### 3.2 关键访问模式

```
A[i][k] 访问模式：
  for i = 0..127:
    a_row = A[i]
    for j = 0..327:     ← a_row 被复用 328 次
      for k = 0..511:   ← 顺序访问
        a_val = a_row[k]
→ a_row 首次加载后全程 L1 驻留（512B ≪ 64 KiB）

B_T[j][k] 访问模式：
  for i = 0..127:
    for j = 0..327:
      b_row = B_T[j]    ← 每次 j 变换加载新行
      for k = 0..511:   ← 顺序访问
        b_val = b_row[k]
→ 每行 512B = 8 个 cache line，硬件预取从 L2 → L1 流式加载
```

### 3.3 对查表延迟的影响

与 exp7 相比，exp9 的每次查表额外包含：

| 额外开销 | 来源 | 预期延迟 |
|---------|------|---------|
| A[i][k] load | A 矩阵行（顺序，L1 命中） | ~4 cyc （与 exp7 的 a_vals 相同） |
| B_T[j][k] load | B_T 矩阵行（顺序，**L2 命中**） | **~12 cyc** vs exp7 的 b_vals L1 ~4 cyc |
| idx 取模 | `A[i][k] % table_a` | ~2-4 cyc（除法器） |

**关键差异**：B_T[j][k] 从 L2 加载，比 exp7 的 L1 加载多 ~8 周期/查表。这是 exp9 与 exp7 的核心区别。

## 4. 实验设置

| 参数 | 值 |
|------|-----|
| 矩阵 | N=128, S=328, L=512（与实验 6/8 一致） |
| 表格式 | BF16（2B/entry） |
| table_a | 2, 4, 8, 16, 32（与 exp7 一致） |
| 子表大小 | 1/2/4/8/16 KiB（完全 L1 驻留） |
| 线程 | 1（单核） |
| 三阶段计时 | 标量查表 / SVE 累加 / SVE 归约 |
| 预热 | 20 次完整 matmul |
| 采样 | 200 次完整 matmul |
| PMU | L1D_CACHE / L1D_CACHE_REFILL |

### 4.1 三阶段计时方式

在每个 (i,j) 对内部插桩，跨所有 (i,j) 累计：

```
for i in 0..127:
    a_row = A + i * L
    for j in 0..327:
        b_row = B_T + j * L

        t0 = omp_get_wtime()
        // 阶段1：标量 A/B 加载 + 子表查表 + BF16→float
        for k in 0..511:
            a_val = a_row[k] % table_a
            idx = a_val * 256 + b_row[k]
            local_vals[k] = BF16→float(sub[idx])
        t1 = omp_get_wtime()

        // 阶段2：SVE 向量化累加
        acc = svdup_n_f32(0)
        svadd_f32_m loop over local_vals[]
        t2 = omp_get_wtime()

        // 阶段3：SVE 归约
        C[i][j] = svaddv_f32(acc)
        t3 = omp_get_wtime()

        sum_scalar   += t1 - t0
        sum_sve_acc  += t2 - t1
        sum_sve_rdc  += t3 - t2
```

使用 `omp_get_wtime()`（~20ns）而非 `high_resolution_clock`（~50ns），将 126k 次调用的计时开销控制在 ~2.5ms/iteration。

## 5. 输出格式

与 exp7 相同的三阶段分解列，查表次数 = N×S×L = 21.5M，新增每次查表(ns) = 总耗时 / (N×S×L)：

```
表维度   表大小(KiB)   查表次数      平均耗时(μs)   每次查表(ns)   L1-miss%   标量(μs)   SVE累加(μs)   归约(μs)
```

同时输出与 exp7 的对比如下：

```
对比 exp7:
  table_a=2:  每次查表 exp7=1.39ns → exp9=? ns  (差值 = B_T L2 加载开销)
  table_a=4:  每次查表 exp7=1.97ns → exp9=? ns
  ...
```

## 6. 预期分析与对比框架

### 6.1 exp9 vs exp7 预期

| table_a | 子表(KiB) | exp7 每次查表(ns) | exp9 预期每次查表(ns) | 主要差异 |
|---------|----------|-----------------|--------------------|--------|
| 2 | 1 | 1.39 | 3.0~4.5 | B_T L2 加载 + 取模 |
| 4 | 2 | 1.97 | 3.5~5.0 | 同上 |
| 8 | 4 | 3.18 | 4.5~6.0 | 同上 |
| 16 | 8 | 5.93 | 7.0~9.0 | 同上 |
| 32 | 16 | 11.38 | 12~15 | 同上 |

（注：exp7 小查表次数时每次查表指标虚高，table_a=2 的 1.39ns 是最接近真实 L1 延迟的值）

### 6.2 exp9 vs 实验 6/8 的关系

| 实验 | 核数 | 查表延迟适用场景 |
|------|------|----------------|
| exp7 | 1 | 纯 L1 查表延迟（理想值） |
| **exp9** | **1** | **真实矩阵加载下的查表延迟** |
| exp6 | G=2~64 | 分组查表（atomic 瓶颈） |
| exp8 | 80 | 行并行 + float 子表（无 atomic） |

exp9 填补了"单核理想值"(exp7)与"多核完整系统"(exp6/8)之间的空白，提供**单核真实环境基线**。

## 7. 预期结论

1. **B_T L2 加载是主要额外开销**：每次查表增加 ~8 周期（比 exp7 高 ~2-3×）
2. **L1-miss% 上升**：B_T 矩阵访问引入额外的 L1 miss（A 行 64KB + B_T 流式 512B/j 竞争 L1）
3. **标量相位比例增加**：A/B 加载 + 取模使标量相位占总时间比例从 ~65%（exp7）升至 ~75-80%
4. **SVE 归约时间不变**：与查表次数无关的固定开销，验证计时正确性

## 8. 代码索引

| 函数 | 说明 |
|------|------|
| `exp9_matrix_load_microbench()` | 主函数：生成矩阵 + table_a 扫描 + 三阶段计时 |
| 三阶段计时 | 与 exp7 相同的 t0/t1/t2/t3 模式，插桩在 (i,j) 循环内 |
