# FP8 查表法矩阵乘法 — Cache 优化实验报告

## 1. 实验背景

### 1.1 查表法（LUT）矩阵乘法原理

在 FP8 精度下，矩阵乘法可以通过查表来模拟。基本思想是：

- 将 FP8 量化值编码为 `uint8_t`（0\~255）
- 预计算一个 **256×256 的查找表（LUT）**，表中存储所有可能的 (idxA, idxB) 组合的乘积近似值
- 计算时直接用两个 FP8 索引拼成地址查表，避免实际浮点乘法

```cpp
// 核心计算：一次查表代替一次乘法
for (int k = 0; k < L; ++k) {
    int offset = (idxA[k] << 8) | idxB[k];   // 拼出 16-bit 索引
    sum += table[offset];                      // 查表累加
}
```

**全量计算的数据集：**

| 数据 | 类型 | 维度 | 大小 |
|------|------|------|------|
| 查找表 LUT | float | 256 × 256 | **256 KiB** |
| 矩阵 A | uint8_t | N × L | N × L × 1 B |
| 矩阵 B_T | uint8_t | S × L | S × L × 1 B |
| 结果 C | float | N × S | N × S × 4 B |

### 1.2 为什么 Cache 优化至关重要

CPU 各层级的访存延迟差异巨大：

| 层级 | 每核大小 | 延迟 | 相对速度 |
|------|---------|------|---------|
| **L1d** | 48 KiB | ~1 ns | 1× |
| **L2** | 1.25 MiB | ~4 ns | 4× |
| **L3**（共享） | 25 MiB | ~12 ns | 12× |
| **DRAM** | — | ~60-100 ns | 60-100× |

**查表法的特殊性：**

LUT 的大小为 **256 KiB**，已经 **远超 L1d（48 KiB）**。这意味着：

- LUT 永远不可能完全驻留在 L1 中
- 每次查表 `table[offset]` 都是**随机 gather 访存**（offset 由数据驱动），无法预取
- 每次查表至少需要 L2 延迟（~4 ns），最差需要 DRAM 延迟（~100 ns）

因此，**让 LUT 尽可能待在 L2 中**是整个优化的核心目标。

### 1.3 分块（Tiling）优化原理

分块的核心思想：**将大矩阵切分成小 tile，使每个 tile 的工作集能放进 cache**。

```
无分块：每个线程处理一整行/一整列 → 工作集大 → cache 放不下 → 频繁 DRAM 访问

分块后：每个线程处理一个小 tile (Ni × Sj) → 工作集可控 → cache 命中率高
```

单 tile 的工作集估算：

```
WorkSet = A_tile + B_T_tile + C_tile + LUT
        = Ni × L × 1 + Sj × L × 1 + Ni × Sj × 4 + 256 KiB
```

例如 tile 32×128：`32×512 + 128×512 + 32×128×4 + 256K = 352 KiB < L2(1280 KiB)` ✓

分块的另一个好处是**多核并行**：不同 tile 分配给不同核心，利用每个核的私有 L2 cache，避免多核竞争共享资源。

---

## 2. 实验设置

### 2.1 硬件环境

| 项目 | 规格 |
|------|------|
| CPU | Intel i7-12700 (Alder Lake) |
| 核心数 | 10 核（8 P-core + 4 E-core） |
| 超线程 | 共 20 逻辑处理器 |
| L1d | 48 KiB / 核 |
| L2 | 1.25 MiB (1280 KiB) / 核 |
| L3 | 25 MiB（所有核共享） |
| 内存 | ~60-100 ns 延迟 |
| 虚拟化 | Microsoft Hyper-V |

> **注意：** 虽然本实验在 x86 平台上运行，但所验证的 cache 优化原理完全适用于 ARM SVE 或其他任何架构。

### 2.2 软件环境

| 项目 | 版本 |
|------|------|
| OS | Linux (WSL2) |
| 编译器 | g++ (GCC) |
| 编译选项 | `-O3 -fopenmp -march=native -std=c++17` |
| 并行框架 | OpenMP |
| 计时 | `std::chrono::high_resolution_clock` |

### 2.3 实验设计

共设计 3 组实验，从不同维度验证 cache 优化效果：

| 实验 | 目的 | 变量 | 固定参数 |
|------|------|------|---------|
| **实验1** | 多核扩展性 | 线程数 1/2/4/6/8/10 | 全量矩阵 N=128, S=328, 无分块 |
| **实验2** | Tile 尺寸扫描 | Ni ∈ {2,4,8,16,32,64,128}, Sj ∈ {8,16,32,64,128,256,328} | 10 线程, N=128, S=328 |
| **实验3** | 矩阵规模扩展 | Scale 1×/2×/4×/8× | 10 线程, 对比 无tile vs tile |

数据集参数：

```
L = 512（内维度固定）
base N = 128, base S = 328
Scale 1×: N=128,  S=328
Scale 2×: N=256,  S=656
Scale 4×: N=512,  S=1312
Scale 8×: N=1024, S=2624
```

### 2.4 性能指标

- **Time (us)**：运行耗时，微秒，取多次运行最小值
- **GOP/s**：每秒十亿次查表操作，`2 × N × S × L / time_us / 1e3`
- **Speedup**：相对于 1 线程的加速比
- **Efficiency**：`Speedup / Threads × 100%`
- **Cache Level**：估算的工作集对应的 cache 层级

---

## 3. 实验结果与分析

### 3.1 实验1：多核扩展性

**条件：** 全量矩阵 N=128, S=328，无分块，工作集 ~648 KiB/线程

```
Threads   Time(us)   Speedup   Efficiency   GOP/s
1         10755      1.00×     100%         2.00
2         5036       2.14×     107%         4.27    ← Turbo Boost
4         3072       3.50×     88%          7.00
6         2479       4.34×     72%          8.67    ← 开始衰减
8         4266       2.52×     32%          5.04    ← E-core 问题
10        2761       3.90×     39%          7.79
```

#### 分析

1. **1→2 线程：超线性加速（107%）**。原因是 Turbo Boost —— 多核负载下 CPU 频率可能提升，加上更多 cache 资源可用。

2. **4→6 线程：效率开始下降**（88% → 72%）。i7-12700 是混合架构（8 P-cores + 4 E-cores），6 线程时可能已有部分线程分配到 E-core，导致效率下降。

3. **8 线程异常：反而比 6 线程慢**。这很可能是 8 个线程中有 2 个被分配到 E-core 上，E-core 的性能远低于 P-core，拖慢了整体进度（OpenMP `schedule(static)` 的负载不均）。

4. **10 线程回到 3.90×**：更多线程掩盖了部分 E-core 的延迟，但依然受限于**内存带宽**。

5. **核心瓶颈**：查表法是 **memory-bound**，每个 GOP 需要一次随机 gather 访存。10 核共享内存带宽，导致加速比远低于计算密集型任务（如 INT8 矩阵乘可能达到 8-10×）。

---

### 3.2 实验2：Tile 尺寸扫描

**条件：** 10 线程，N=128, S=328，扫描不同 (Ni, Sj) 组合

**完整结果：**

```
Ni   Sj   TileWorkSet  CacheLvl  Time(us)  GOP/s
2    8    261 KiB      L2        1192      18.02  ← 最优
2    16   265 KiB      L2        1345      15.97
4    8    262 KiB      L2        1268      16.95
4    16   266 KiB      L2        1456      14.76
8    8    264 KiB      L2        1441      14.91
8    16   268 KiB      L2        1272      16.90
16   8    268 KiB      L2        1405      15.29
32   8    277 KiB      L2        1227      17.51
...
128  328  648 KiB      L2        10070     2.13   ← 最差
```

#### 关键发现

1. **所有 tile 的 CacheLvl 都是 L2**（最大 648 KiB < 1280 KiB）。因此本实验中看不到 cache miss 差异，tile 大小的影响主要来自**并行度**。

2. **小 tile（Ni=2, Sj=8）性能最好（18.02 GOP/s）**。Tile 数量 = `(128/2) × (328/8) = 64×41 = 2624 个`，远超 10 线程，OpenMP 可以充分负载均衡。

3. **大 tile（Ni=128, Sj=328）性能最差（2.13 GOP/s）**。只有 1 个 tile，等同于单线程。

4. **GOP/s 与 tile 数量正相关**：

   ```
   tile 数量         GOP/s
   1                 2.13   (1 tile = 串行)
   16                8.53   (Ni=16, Sj=256)
   64                14.69  (Ni=128, Sj=8)
   2624              18.02  (Ni=2, Sj=8)
   ```

5. **Sj 的影响大于 Ni**：固定 Ni=8，Sj 从 8→328 时，GOP/s 从 14.91 降到 11.51。这是因为 Sj 增大 → B_T tile 增大 → 每 tile 工作集增大 → 更多 cache 压力。

#### 小结

在数据量小（全部在 L2 内）的情况下，tile 优化的主要作用是**提高并行度**。更大的矩阵规模才能体现 cache miss 减少的收益（见实验3）。

---

### 3.3 实验3：矩阵规模扩展 × Tile 优化

这是**核心实验**，验证 tiling 在大矩阵下通过减少 cache miss 带来的性能收益。

```
Scale  工作集    Cache    Threads  Tiling    Time(us)   GOP/s
─────────────────────────────────────────────────────────────
1×     648 KiB  L2       1        none      10376      2.07
1×     648 KiB  L2       10       none      2692       7.98
1×     352 KiB  L2       10       32×128    1933       11.12  ← tile +39%
─────────────────────────────────────────────────────────────
2×     1.3 MiB  >L2      1        none      40174      2.14
2×     1.3 MiB  L3       10       none      8335       10.31
2×     352 KiB  L2       10       32×128    5388       15.96  ← tile +55% (最佳)
─────────────────────────────────────────────────────────────
4×     3.7 MiB  DRAM     1        none      165647     2.08
4×     3.7 MiB  DRAM     10       none      30493      11.28
4×     352 KiB  L2       10       32×128    22522      15.27  ← tile +35%
─────────────────────────────────────────────────────────────
8×     12.3 MiB DRAM     1        none      656100     2.10
8×     12.3 MiB DRAM     10       none      129800     10.60
8×     352 KiB  L2       10       32×128    87910      15.65  ← tile +48%
```

#### 核心结论

**① 单线程性能与问题规模无关（~2 GOP/s）**

所有 scale 下，单线程都稳定在 ~2 GOP/s。这说明查表法单线程 **完全受限于 L2→L3/DRAM 带宽**，增加计算量不会降低吞吐率。

**② 无 tile 时，10 线程性能随规模增大先升后降**

- 1×（648 KiB，L2）：7.98 GOP/s — 受限于 L2 带宽
- 2×（1.3 MiB，L3）：10.31 GOP/s — 全部在 L3 内，L3 带宽大于 L2 带宽（多核共享）
- 4×（3.7 MiB，DRAM）：11.28 GOP/s — 少量 DRAM 访问，仍在 L3 部分命中
- 8×（12.3 MiB，DRAM）：10.60 GOP/s — 大量 DRAM 访问，性能下降

**③ 加 tile 后稳定在 ~15-16 GOP/s（提升 35-55%）**

无论矩阵多大，tiled 版本始终将工作集控制在 **352 KiB（32×128 tile）**，确保每个线程的所有数据（包括 LUT）都在 L2 内。结果稳定在 15-16 GOP/s。

| Scale | 无 tile (GOP/s) | 有 tile (GOP/s) | 提升幅度 |
|-------|-----------------|----------------|---------|
| 1× | 7.98 | 11.12 | **+39%** |
| 2× | 10.31 | 15.96 | **+55%** |
| 4× | 11.28 | 15.27 | **+35%** |
| 8× | 10.60 | 15.65 | **+48%** |

**④ 8× 仍无法达到 2× 的 15.96 GOP/s（15.65 vs 15.96）**

8× scale 下 10 核共享 25 MiB L3，10 个线程 × 352 KiB tile 工作集 = 3.44 MiB 瞬时活跃数据，加上 LUT 的随机 gather 会产生 L3 冲突，微弱的性能损失是正常的。

---

## 4. 总结

### 4.1 优化收益

| 优化措施 | 相对基线（1线程） | 相对10线程无tile |
|---------|-----------------|-----------------|
| 10 线程并行 | **~3-5×** | — |
| + Tile 分块 | — | **+35-55%** |
| 最优组合 | **~8×** | — |

### 4.2 最佳实践建议

1. **Tile 尺寸选择**：对于本问题（LUT 256 KiB > L1d），推荐 Ni=32, Sj=128（工作集 352 KiB），充分使用 L2 同时保持足够 tile 数量。

2. **多核适用性判断**：查表法是 **memory-bound**，加速比受限于内存带宽，一般 4-8 核即可接近带宽上限，继续增加核心收益递减。

3. **LUT 本身的优化方向**（进一步突破）：
   - 将 LUT 从 float 降为 `__fp16`（128 KiB）—— 仍 > L1d 但 L2 压力减半
   - 两级查表：先粗查再精调
   - 硬件原生 FP8 乘加指令（如果 CPU 支持）

4. **跨架构适用性**：本文所有 cache 优化原理同样适用于 ARM SVE 或 GPU 平台 —— 只需将标量内循环替换为对应的向量化指令（如 SVE gather）。

### 4.3 Cache Miss 验证

可通过 `perf` 工具直接测量硬件 cache miss 事件，验证上述分析：

```bash
# 对比有/无 tile 时的 L2 miss 次数
perf stat -e L1-dcache-load-misses,l2d_cache_refill,LLC-load-misses \
  OMP_NUM_THREADS=10 ./matmul_fp8_test
```

---

## 附录：代码结构

```
matmul_fp8_lookup_test.cpp
├── 数据加载/生成
│   ├── load_or_generate()     — 加载 .bin 或生成随机数据
│   └── gen_lut()              — 生成合成查找表
├── 计算核心
│   ├── lookup_scalar()        — 标量版（串行基线）
│   ├── lookup_scalar_omp()    — OpenMP 行并行
│   └── lookup_scalar_tiled_omp() — 分块 + OpenMP
├── Cache 分析工具
│   ├── calc_tile_workset_kib()
│   ├── cache_level()
│   └── format_size_kib() / size_unit()
├── 实验函数
│   ├── experiment_thread_scaling()
│   ├── experiment_tile_sweep()
│   └── experiment_matrix_scaling()
└── main()
```

编译运行：

```bash
g++ -O3 -fopenmp -march=native -o matmul_fp8_test matmul_fp8_lookup_test.cpp -std=c++17
OMP_NUM_THREADS=10 OMP_PROC_BIND=true ./matmul_fp8_test
```
