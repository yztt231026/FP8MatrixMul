# 实验 7：单核 L1 小表查表微基准（SVE 归约）

## 1. 核心思路

### 1.1 问题

实验 6 的多核分组方案中，atomic 竞争和负载不均掩盖了 L1 查表的真实延迟收益。需要一个**单核微基准**来直接测量 L1 驻留 BF16 子表的查表延迟，排除多核干扰。

### 1.2 方案

构建一个极小的 BF16 表（2×256 = 1 KiB），完全驻留 L1d。执行 128 次随机查表，用 SVE 向量化累加 + `svaddv` 归约汇总结果。通过大量迭代取最小值，估算单次 L1 查表的延迟（ns 和周期数）。

```
表大小: 2×256 BF16 = 512 entries × 2B = 1 KiB << L1d 64 KiB
查表: 128 次随机 (a, b) 对，a ∈ [0,1], b ∈ [0,255]
累加: SVE svadd_f32_m 向量化累加 + svaddv_f32 归约
测量: 2000 warmup + 20000 次采样，取最小值
```

### 1.3 与实验 6 的关系

| 实验 | 表大小 | 核数 | 测量目标 |
|------|--------|------|---------|
| 实验 6 | 4~128 KiB | 1~64 | 多核分组 vs 行并行基线 |
| **实验 7** | **1 KiB** | **1** | **纯净 L1 查表延迟下限** |

实验 7 排除所有多核干扰（atomic 竞争、负载不均、MESI 一致性流量），给出 L1 查表延迟的理论下限。

---

## 2. 数据流

```
构建 BF16 表 (2×256, 1 KiB)：
  for a in [0,1], b in [0,255]:
    sub[a*256+b] = float_to_bf16(sin(a*0.1) * cos(b*0.1))

生成 128 对随机 (a, b)：
  a_vals[i] = rand() & 1  (clamp to [0,1])
  b_vals[i] = rand() % 256

阶段1（标量查表 + BF16→float）：
  for i in 0..127:
    idx = a_vals[i] * 256 + b_vals[i]
    bits = (uint32_t)sub[idx] << 16    // BF16 → float (高16位填充)
    local_vals[i] = bits_as_float

阶段2（SVE 向量化累加 + 归约）：
  acc = svdup_n_f32(0.0f)
  for t in 0..127 step 8:
    acc = svadd_f32_m(pg, acc, svld1_f32(pg, local_vals + t))
  result = svaddv_f32(svptrue_b32(), acc)
```

### 2.1 BF16→float 转换

```cpp
uint32_t bits = (uint32_t)sub[idx] << 16;  // BF16 在低16位，左移16至高16位
float v;
memcpy(&v, &bits, 4);  // 解释为 IEEE 754 float32
```

BF16 的符号位、指数位、尾数位刚好对齐 float32 的高 16 位，左移 16 位后低位补零即得到正确的 float32 值（尾数精度降低，但指数范围一致）。

### 2.2 SVE 归约

```
svaddv_f32(svptrue_b32(), acc)
    ↑           ↑            ↑
  归约指令    全真谓词      SVE 向量寄存器
  (水平加和)  (所有通道)    (8个float)

一条指令将向量寄存器的 8 个 float 两两相加，最终归约为标量。
```

---

## 3. 核函数设计

### 3.1 代码

```cpp
constexpr int TABLE_A = 2;         // idxA 范围 [0,1]
constexpr int TABLE_B = 256;       // idxB 范围 [0,255]
constexpr int N_LOOKUPS = 128;     // 查表次数

// 阶段1：标量查表 + BF16→float
alignas(64) float local_vals[128];
for (int i = 0; i < N_LOOKUPS; ++i) {
    int idx = a_vals[i] * TABLE_B + b_vals[i];
    uint32_t bits = (uint32_t)sub[idx] << 16;
    memcpy(&local_vals[i], &bits, 4);
}

// 阶段2：SVE 向量化累加 + svaddv 归约
svfloat32_t acc = svdup_n_f32(0.0f);
int t = 0;
svbool_t pg = svwhilelt_b32(t, N_LOOKUPS);
while (svptest_any(svptrue_b32(), pg)) {
    acc = svadd_f32_m(pg, acc, svld1_f32(pg, local_vals + t));
    t += svcntw();
    pg = svwhilelt_b32(t, N_LOOKUPS);
}
float result = svaddv_f32(svptrue_b32(), acc);
```

### 3.2 关键实现细节

**`svadd_f32_m` vs `svadd_f32_z`：**

必须使用 `_m`（merge 谓词）而非 `_z`（zeroing 谓词）。128 不能被 `svcntw()`（=8）整除时，最后一批处理的部分向量（第 128 个元素后无多余元素，此处恰好 128 ÷ 8 = 16 批无余数）——实际上 128 是 8 的倍数，所以没有尾数问题。但保留 `_m` 写法以保持与实验 6 一致，也是通用最佳实践。

**`volatile` 防止优化：**

```cpp
volatile float sink = 0;
// ...
sink = svaddv_f32(svptrue_b32(), acc);
```

`volatile` 阻止编译器将计算结果完全优化掉，确保 `svaddv` 的指令实际执行。

**`alignas(64)` 栈对齐：**

`local_vals` 声明为 `alignas(64)`，对齐到 cache line 边界（64 字节），避免跨行访问的额外延迟。

### 3.3 计时方式

```
t0                        t1
├─────────────────────────┤
│ 阶段1 + 阶段2 (128次)    │
└─────────────────────────┘
→ us = (t1 - t0) in ns / 1000

指标：
  耗时(min)          = 20000 次中的最优（最小）值
  每次查表(ns)       = 耗时(min) × 1000 / 128
  单次查表延迟(cyc)  = 每次查表(ns) × 2.4 GHz
```

---

## 4. 实验设置

| 参数 | 值 |
|------|-----|
| 表维度 | 2×256（idxA ∈ [0,1], idxB ∈ [0,255]） |
| 表格式 | BF16（2B/entry），= 1 KiB |
| Cache 层级 | **完全 L1 驻留**（1 KiB << 64 KiB L1d） |
| 查表次数 | 128 次 |
| 线程 | 1（单核） |
| 编译器 | `g++ -O3 -fopenmp -march=armv8.2-a+sve` |
| 平台 | HiSilicon Kunpeng, 256-bit SVE, L1d=64KiB |
| 预热 | 2000 次 |
| 采样 | 20000 次 |
| 计时器 | `std::chrono::high_resolution_clock`（ns 精度） |

### 4.1 输出指标

| 指标 | 含义 |
|------|------|
| 耗时 mean±sd | 20000 次采样的平均值 ± 标准差 |
| 耗时 min | 20000 次采样中的最小值（排除 OS 抖动） |
| 每次查表 (ns) | min / 128 × 1000 |
| 单次查表延迟 (cyc) | 每次查表 ns × 2.4 GHz |

---

## 5. 预期分析

### 5.1 L1 查表延迟的理论值

鲲鹏 920 典型延迟：
- L1 命中加载：~4 周期
- BF16→float 转换（左移）：1 周期
- SVE `svadd_f32_m`：~4 周期（8 个 float 并行）
- `svaddv_f32` 归约：~5 周期

128 次查表预期总时间（忽略循环开销）：
```
阶段1：128 × (L1加载4 + 移位1 + 存储1)  ≈ 768 周期
阶段2：16 批 × 4 周期 + 归约 5 周期     ≈ 69 周期
合计     ≈ 837 周期 ≈ 349 ns (@2.4 GHz)
每次查表 ≈ 6.5 周期 ≈ 2.7 ns
```

### 5.2 与实验 6 的对比

| 场景 | 表大小 | 每次查表延迟 | 影响因素 |
|------|--------|-------------|---------|
| 全表 L2（实验 6 基线） | 256 KiB | ~12 周期 | L2 加载 + gather 地址计算 |
| G=32 分组（实验 6） | 4 KiB | ~4 周期 + atomic 开销 | L1 命中 + atomic 竞争 |
| **实验 7（单核纯净）** | **1 KiB** | **~6-8 周期** | **纯 L1 查表 + SVE 累加** |

实验 7 的值应接近 L1 查表延迟的理论下限，为实验 6 的 G=32/64 结果提供"无竞争时的理想 L1 延迟"参照。

### 5.3 与实验 6 基线（SVE gather）的对比

实验 6 基线使用 `svld1_gather_u32index_f32`（SVE gather 加载），单次查表约 12 周期。实验 7 的标量查表 + SVE 累加将查表与累加分离，可以观察：
1. **标量 L1 加载 vs SVE gather L2 加载**的延迟差异
2. **SVE 向量累加 vs 标量累加**的吞吐差异

---

## 6. 代码索引

| 函数 | 行号 | 说明 |
|------|------|------|
| `exp7_single_core_l1_lookup()` | 573-678 | 主函数：建表 + 查表 + SVE 归约 + 计时 |
| `float_to_bf16()` | 189-193 | float32→BF16 转换 |
| `fill_random()` | 48-50 | 随机数据生成 |

### 编译运行

```bash
g++ -O3 -fopenmp -march=armv8.2-a+sve -o fp8_server \
    matmul_fp8_server.cpp -std=c++17 -lnuma

# 只运行实验 7
./fp8_server 7
```
