# 实验 7：单核 L1 小表查表微基准（SVE 归约）

## 1. 核心思路

### 1.1 问题

实验 6 的多核分组方案中，atomic 竞争和负载不均掩盖了 L1 查表的真实延迟收益。需要一个**单核微基准**来直接测量 L1 驻留 BF16 子表的查表延迟，排除多核干扰。

### 1.2 方案

构建一系列 BF16 表（2/4/8/16/32 × 256，全 L1 驻留）。查表次数与表维度成反比（表越大每行覆盖的 idxA 范围越大，查表次数越少），始终保持总数据量为满行 256 个 idxA 值对应一次完整覆盖。

```
表维度       表大小     查表次数     原因
2×256        1 KiB      128          2 row × 256 col, 每行 256 个 b, 查 128 次
4×256        2 KiB      64           4 row × 256 col, 每行 256 个 b, 查 64 次
8×256        4 KiB      32           8 row × 256 col, 每行 256 个 b, 查 32 次
16×256       8 KiB      16           16 row × 256 col, 每行 256 个 b, 查 16 次
32×256      16 KiB      8            32 row × 256 col, 每行 256 个 b, 查 8 次

查表次数 = 128 / (table_a / 2) = 256 / table_a
```

每个表大小都远小于 L1d 64 KiB：
- 查表: (a, b) 对，a ∈ [0, table_a-1], b ∈ [0,255]
- 累加: SVE `svadd_f32_m` 向量化累加 + `svaddv_f32` 归约
- 测量: 2000 warmup + 20000 次采样，**计算平均值**
- PMU: 同时采集 L1D_CACHE / L1D_CACHE_REFILL，计算 **L1-miss%**

### 1.3 与实验 6 的关系

| 实验 | 表大小 | 核数 | 测量目标 |
|------|--------|------|---------|
| 实验 6 | 4~128 KiB | 1~64 | 多核分组 vs 行并行基线 |
| **实验 7** | **1~16 KiB** | **1** | **L1 查表延迟 + L1-miss% vs 表大小** |

实验 7 排除所有多核干扰（atomic 竞争、负载不均、MESI 一致性流量），直接观察 L1 查表延迟和 L1 miss 率如何随表大小变化。

---

## 2. 数据流

```
构建 BF16 表 (table_a × 256)：
  for a in [0, table_a-1], b in [0,255]:
    sub[a*256+b] = float_to_bf16(sin(a*0.1) * cos(b*0.1))

生成查表次数组 (a, b)：
  n_lookups = 256 / table_a
  a_vals[i] = rand() % table_a
  b_vals[i] = rand() % 256

阶段1（标量查表 + BF16→float）：
  for i in 0..n_lookups-1:
    idx = a_vals[i] * 256 + b_vals[i]
    bits = (uint32_t)sub[idx] << 16    // BF16 → float (高16位填充)
    local_vals[i] = bits_as_float
  t1 = now()

阶段2（SVE 向量化累加）：
  acc = svdup_n_f32(0.0f)
  for t in 0..n_lookups-1 step 8:
    acc = svadd_f32_m(pg, acc, svld1_f32(pg, local_vals + t))
  t2 = now()

阶段3（SVE 归约）：
  sink = svaddv_f32(svptrue_b32(), acc)  // 水平加和 → 标量
  t3 = now()

各阶段分别累加：(t1-t0), (t2-t1), (t3-t2)，20000 次取平均
```

### 2.1 BF16→float 转换

```cpp
uint32_t bits = (uint32_t)sub[idx] << 16;  // BF16 在低16位，左移16至高16位
float v;
memcpy(&v, &bits, 4);  // 解释为 IEEE 754 float32
```

BF16 的符号位、指数位、尾数位刚好对齐 float32 的高 16 位，左移 16 位后低位补零即得到正确的 float32 值（尾数精度降低，但指数范围一致）。

### 2.2 SVE 归约

```
svaddv_f32(svptrue_b32(), acc)
    ↑           ↑            ↑
  归约指令    全真谓词      SVE 向量寄存器
  (水平加和)  (所有通道)    (8个float)

一条指令将向量寄存器的 8 个 float 两两相加，最终归约为标量。

### 2.3 三阶段计时

每次迭代在三个时间点插桩（t0/t1/t2/t3），分别累加三个阶段的耗时：

```
t0          t1          t2          t3
├────────────┼────────────┼────────────┤
│ 阶段1      │ 阶段2      │ 阶段3      │
│ 标量查表   │ SVE累加    │ SVE归约    │
│ BF16→float │ svadd_f32_m│ svaddv_f32 │
└────────────┴────────────┴────────────┘

指标：
  平均总耗时(μs) = 20000 次 (t3-t0) 总和 / 20000
  平均标量(μs)   = 20000 次 (t1-t0) 总和 / 20000
  平均SVE累加(μs)= 20000 次 (t2-t1) 总和 / 20000
  平均归约(μs)   = 20000 次 (t3-t2) 总和 / 20000
```

虽然 `svaddv_f32` 单条指令仅 ~5 周期，20000 次平均后仍可得到稳定测量值。
```

---

## 3. 核函数设计

### 3.1 代码

```cpp
constexpr int TABLE_B = 256;
constexpr int MAX_LOOKUPS = 128;    // 最大查表次数

alignas(64) float local_vals[MAX_LOOKUPS];  // 预分配最大空间

for (int table_a : {2, 4, 8, 16, 32}) {
    int n_lookups = MAX_LOOKUPS / (table_a / 2);

    // 构建 BF16 表
    std::vector<uint16_t> sub(table_a * TABLE_B);
    for (int a = 0; a < table_a; ++a)
        for (int b = 0; b < TABLE_B; ++b)
            sub[a * TABLE_B + b] = float_to_bf16(...);

    // 生成随机 (a, b) 对
    uint8_t a_vals[n_lookups], b_vals[n_lookups];
    fill_random(a_vals, n_lookups);
    for (auto &v : a_vals) v %= table_a;
    fill_random(b_vals, n_lookups);

    // Warmup 2000 次
    for (int w = 0; w < 2000; ++w) {
        for (int i = 0; i < n_lookups; ++i) {
            int idx = a_vals[i] * TABLE_B + b_vals[i];
            uint32_t bits = (uint32_t)sub[idx] << 16;
            memcpy(&local_vals[i], &bits, 4);
        }
        // SVE 累加 + 归约（同下）
    }

    // PMU 计数使能
    PerfCounter pc_l1_acc(L1D_CACHE);
    PerfCounter pc_l1_miss(L1D_CACHE_REFILL);
    pc_l1_acc.enable();  pc_l1_miss.enable();

    // 正式测量 20000 次，分阶段统计时间
    double sum_total = 0, sum_scalar = 0, sum_sve_acc = 0, sum_sve_reduce = 0;
    for (int iter = 0; iter < 20000; ++iter) {
        auto t0 = now();

        // 阶段1: 标量查表 + BF16→float
        for (int i = 0; i < n_lookups; ++i) {
            int idx = a_vals[i] * TABLE_B + b_vals[i];
            uint32_t bits = (uint32_t)sub[idx] << 16;
            memcpy(&local_vals[i], &bits, 4);
        }
        auto t1 = now();

        // 阶段2: SVE 向量化累加 (svadd_f32_m)
        svfloat32_t acc = svdup_n_f32(0.0f);
        int t = 0;
        svbool_t pg = svwhilelt_b32(t, n_lookups);
        while (svptest_any(svptrue_b32(), pg)) {
            acc = svadd_f32_m(pg, acc, svld1_f32(pg, local_vals + t));
            t += svcntw();
            pg = svwhilelt_b32(t, n_lookups);
        }
        auto t2 = now();

        // 阶段3: SVE 归约 (svaddv_f32)
        sink = svaddv_f32(svptrue_b32(), acc);
        auto t3 = now();

        sum_scalar   += (t1 - t0);
        sum_sve_acc  += (t2 - t1);
        sum_sve_reduce += (t3 - t2);
        sum_total    += (t3 - t0);
    }

    pc_l1_acc.disable();  pc_l1_miss.disable();

    double avg_us = sum_us / 20000;
    double l1_miss_rate = 100.0 * pc_l1_miss.read() / pc_l1_acc.read();
}
```

### 3.2 关键实现细节

**`svadd_f32_m` vs `svadd_f32_z`：**

必须使用 `_m`（merge 谓词）而非 `_z`（zeroing 谓词）。当 `n_lookups` 不能被 `svcntw()`（=8）整除时，最后一批处理的部分向量中，`_z` 会将不活跃通道置零，丢失之前累加的值。`_m` 保留不活跃通道的原值。

**`volatile` 防止优化：**

```cpp
volatile float sink = 0;
sink = svaddv_f32(svptrue_b32(), acc);
```

`volatile` 阻止编译器将计算结果完全优化掉，确保查表和归约指令实际执行。

**`alignas(64)` 栈对齐：**

`local_vals` 声明为 `alignas(64)`，对齐到 cache line 边界（64 字节），避免跨行访问的额外延迟。最大 128 float = 512 字节，全部在栈上。

### 3.3 PMU 测量

使用 `PerfCounter` 类封装 `perf_event_open` 系统调用，读取 ARM PMU 硬件计数器：

```cpp
PerfCounter pc_l1_acc(ArmPmu::L1D_CACHE);        // 事件 0x04
PerfCounter pc_l1_miss(ArmPmu::L1D_CACHE_REFILL); // 事件 0x03

pc_l1_acc.enable();    // PMU 开始计数
// ... 20000 次查表 ...
pc_l1_acc.disable();   // PMU 停止

uint64_t acc  = pc_l1_acc.read();    // L1 总访问次数
uint64_t miss = pc_l1_miss.read();   // L1 缺失次数
double rate = 100.0 * miss / acc;    // L1-miss%
```

计数器从第一次测量开始累计到第 20000 次结束，给出全局平均 miss 率。

### 3.4 计时方式

```
t0          t1          t2          t3
├────────────┼────────────┼────────────┤
│ 阶段1      │ 阶段2      │ 阶段3      │
│ 标量查表   │ SVE累加    │ SVE归约    │
│ (for循环)  │ (while循环) │ (svaddv)   │
└────────────┴────────────┴────────────┘

指标：
  平均总耗时(μs)   = 20000 次 (t3-t0) 总和 / 20000
  平均标量(μs)     = 20000 次 (t1-t0) 总和 / 20000
  平均SVE累加(μs)  = 20000 次 (t2-t1) 总和 / 20000
  平均SVE归约(μs)  = 20000 次 (t3-t2) 总和 / 20000
  每次查表(ns)     = 平均总耗时(ns) / n_lookups

---

## 4. 实验设置

| 参数 | 值 |
|------|-----|
| 表维度 | 2/4/8/16/32 × 256 |
| 表格式 | BF16（2B/entry），= 1~16 KiB |
| Cache 层级 | **完全 L1 驻留**（最大 16 KiB << 64 KiB L1d） |
| 查表次数 | 128 / 64 / 32 / 16 / 8（256 ÷ table_a） |
| 线程 | 1（单核） |
| 编译器 | `g++ -O3 -fopenmp -march=armv8.2-a+sve` |
| 平台 | HiSilicon Kunpeng, 256-bit SVE, L1d=64KiB |
| 预热 | 2000 次 |
| 采样 | 20000 次（**取平均值**） |
| 计时器 | `std::chrono::high_resolution_clock`（ns 精度） |
| PMU | `L1D_CACHE` / `L1D_CACHE_REFILL` 硬件计数器 |

### 4.1 输出格式

```
表维度       表大小(KiB)  查表次数  平均耗时(μs)  总查表(ns)  每次查表(ns)  L1-miss%  标量(μs)  SVE累加(μs)  归约(μs)
----------------------------------------------------------------------------------------------------------------
2×256        1            128       x.xxxx        x.xx        x.xx         x.xx%      x.xxxx    x.xxxx       x.xxxx
4×256        2            64        ...
8×256        4            32
16×256       8            16
32×256       16           8
```

各阶段时间分别统计，验证 SVE 向量化累加和归约的开销占比。

---

## 5. 预期分析

### 5.1 查表次数与总数据量的关系

```
table_a=2:  查表 128 次，覆盖 2 个 idxA 值各 64 次 ≈ 128 次随机访问
table_a=4:  查表 64 次，覆盖 4 个 idxA 值各 16 次 ≈ 64 次随机访问
...
table_a=32: 查表 8 次，覆盖 32 个 idxA 值各 ~0.25 次 ≈ 8 次随机访问
```

每个表大小下，查表次数递减但每次查表的 idxA 范围增大。这模拟了实验 6 中不同 G 值下每核处理的元素数变化。

### 5.2 理论延迟

鲲鹏 920 典型延迟：
- L1 命中加载：~4 周期
- BF16→float 转换（左移）：1 周期
- SVE `svadd_f32_m`：~4 周期（8 个 float 并行）
- `svaddv_f32` 归约：~5 周期

128 次查表预期各阶段时间（忽略循环开销）：
```
阶段1（标量查表）：128 × (L1加载4 + 移位1 + 存储1) ≈ 768 周期
阶段2（SVE累加）：  16 批 × 4 周期                    ≈ 64 周期
阶段3（SVE归约）：  1 条指令                          ≈ 5 周期
合计               ≈ 837 周期 ≈ 349 ns (@2.4 GHz)
每次查表           ≈ 6.5 周期 ≈ 2.7 ns
```

阶段占比预期（table_a=2, 128 次查表）：
| 阶段 | 周期 | 占比 | 说明 |
|------|------|------|------|
| 标量查表 | 768 cyc | **91.8%** | 查表为主，串行执行 |
| SVE 累加 | 64 cyc | 7.6% | 向量化，每批处理 8 个 |
| SVE 归约 | 5 cyc | 0.6% | 单条指令，开销极小 |

随表增大（查表次数减少），阶段 2/3 的占比会进一步下降，阶段 1 占绝对主导。

### 5.3 表大小对延迟和 L1-miss 的影响

| 表大小 | 预期每次查表 | 预期 L1-miss | 说明 |
|--------|-------------|-------------|------|
| 1 KiB | ~6.5 cyc | ~0% | 完全 L1，TLB 无压力 |
| 2 KiB | ~6.5 cyc | ~0% | 同上 |
| 4 KiB | ~6.5 cyc | ~0% | 同上 |
| 8 KiB | ~6.5 cyc | ~0% | L1 占用 1/8，组相联无冲突 |
| 16 KiB | ~6.5-7 cyc | ~0.1%+ | 可能轻微组相联冲突（占 1/4 的 way） |

所有表大小都远小于 L1d 64 KiB，理论上 L1-miss% 应接近零（仅栈 `local_vals` 首次访问和 OS 抖动的底噪）。若有明显升高，说明 L1 组相联冲突或 TLB 压力开始起作用。

### 5.4 与实验 6 的对比

| 场景 | 表大小 | 每次查表延迟 | 影响因素 |
|------|--------|-------------|---------|
| 全表 L2（实验 6 基线） | 256 KiB | ~12 周期 | L2 加载 + gather 地址计算 |
| G=32 分组（实验 6） | 4 KiB | ~4 周期 + atomic 开销 | L1 命中 + atomic 竞争 |
| **实验 7** | **1~16 KiB** | **~6.5 周期** | **纯 L1 查表 + SVE 累加** |

实验 7 的 L1-miss% 和延迟为实验 6 提供"理想 L1 状态"的基线，帮助分离 atomic 竞争、MESI 一致性流量等因素带来的额外开销。

---

## 6. 代码索引

| 函数 | 行号 | 说明 |
|------|------|------|
| `exp7_single_core_l1_lookup()` | 573-700 | 主函数：扫描表大小 + 三阶段计时 + PMU 采集 + 输出 |
| `float_to_bf16()` | 189-193 | float32→BF16 转换 |
| `fill_random()` | 48-50 | 随机数据生成 |
| `PerfCounter` | 146-181 | `perf_event_open` 封装，采集 PMU 硬件事件 |

### 编译运行

```bash
g++ -O3 -fopenmp -march=armv8.2-a+sve -o fp8_server \
    matmul_fp8_server.cpp -std=c++17 -lnuma

# 只运行实验 7
./fp8_server 7
```
