# main.cpp 动态指令计数实验报告

## 1. 实验目的

对 main.cpp 中 6 个矩阵乘法核函数，通过代码插桩精确测量运行时动态指令执行次数，按功能分类量化分析各 kernel 的指令构成差异，从指令级解释 FP8 查表法为何慢于 FP16/INT8 直接乘法。

## 2. 测试环境

| 项目 | 配置 |
|------|------|
| CPU | 鲲鹏 920 (Kunpeng 920) |
| SVE 向量宽度 | 256-bit |
| svcntw() / svcnth() / svcntb() | 8 / 16 / 32 (×128 = 256-bit) |
| L1d Cache | 64 KiB |
| L2 Cache | 1280 KiB |
| 编译选项 | `-O3 -march=armv8.2-a+sve+fp16+i8mm` |
| 矩阵尺寸 | N=128, S=328, L=512 |
| 总 (i,j) 对数 | 128 × 328 = 41,984 |
| 循环次数 (loopCnt) | 10,000 |

## 3. 测试方法：代码插桩（方案 C）

在每个核函数的热循环中插入轻量级计数器，统计以下指标：

| 计数器 | 含义 |
|--------|------|
| `loop_iters` | 最内层热循环迭代次数 |
| `elem_processed` | 处理的矩阵元素总数（= L × (i,j) 对数） |
| `reduce_count` | 向量归约（faddv/saddv）执行次数 |
| `gather_count` | SVE gather 查表指令数（FP8 专用） |
| `compute_count` | 核心计算指令数（fmla / sdot / smmla） |

计数器使用 `uint64_t` 累加，覆盖 loopCnt × N × S 次执行，不改变编译器优化行为。

## 4. 原理

### 4.1 核心公式

动态指令计数基于以下基本关系：

```
动态指令总数(T) = ∑(静态指令数/迭代 × 迭代次数)
                = 热路径指令 + 函数框架指令 + 标量循环控制指令
```

对于矩阵乘法 kernel，三层循环结构如下：

```
for i in [0, N):            ← 外层行循环 (N=128)
  for j in [0, S):          ← 中层列循环 (S=328)
    for k in [0, L):        ← 内层归约循环 (L=512)
      热路径指令体
```

总 (i,j) 对数 = N × S = 128 × 328 = **41,984**

每 (i,j) 对的内循环迭代次数由**向量宽度**决定：

```
iter_per_ij = ceil(L / vector_width)
```

| Kernel | 数据类型 | SVE 指令 | 向量宽度 | vector_width | iter_per_ij |
|--------|---------|---------|:-------:|:-----------:|:----------:|
| FP8 scalar | uint8 (标量) | 无 | 1 | 1 | **512** |
| FP8 SVE | uint32 index | ld1ub_u32 → z32 | 8 | svcntw() | **64** |
| FP16 SVE | float16 | ld1_f16 | 16 | svcnth() | **32** |
| INT8 SVE | int8 | ld1_s8 | 32 | svcntb() | **16** |
| INT8 I8MM | int8 (2×2 tile) | ld1_s8 ×4 | 128 | svcntb()×4 | **4** |

### 4.2 各 Kernel 的热路径指令分解

每迭代的汇编指令直接从 objdump 反汇编结果提取，以下展示各 kernel 最内层循环体的指令构成。

#### 4.2.1 FP8 标量查表（`matmul_fp8_lookup_scalar`）

每 (i,j) 迭代 512 次，每次处理 1 个元素：

```
内循环体（标量，无向量化）：
  ldrb    wX, [rowA, k]        ← 加载 A[k] (uint8)
  ldrb    wX, [rowB, k]        ← 加载 B[k] (uint8)
  lsl     wX, wX, #8           ← idxA << 8
  orr     wX, wX, wX           ← 拼接索引
  ldr     sX, [table, xX, lsl #2]  ← 查表（标量 load，非 gather）
  fcvt    sX, sX               ← 类型转换
  fadd    sX, sX, sX           ← 累加
  add     k, k, #1             ← k++
  cmp     k, L                 ← 循环测试
  b.ne    loop                 ← 分支
```

**每迭代指令数：9 条**（全部标量），但查表是顺序 `ldr`（地址规律），非随机 gather。

#### 4.2.2 FP8 SVE 查表（`matmul_fp8_lookup_sve`）

每 (i,j) 迭代 64 次，每次处理 8 个元素：

```
内循环体（SVE 256-bit）：
  whilelt     p0.s, Xk, XL             ← 谓词生成      1 条
  ld1b        Z0.s, p0/z, [XrowA, Xk]  ← 加载 A 索引   1 条  ← svld1ub_u32
  ld1b        Z1.s, p0/z, [XrowB, Xk]  ← 加载 B 索引   1 条
  lsl         Z0.s, p0/m, Z0.s, #8     ← 左移          1 条
  orr         Z1.s, p0/m, Z0.s, Z1.s   ← 拼接索引      1 条
  ld1w        Z2.s, p0/z, [Xtable, Z1.s, uxtw #2]  ← 查表 gather  1 条
  fadd        Z3.s, p0/m, Z3.s, Z2.s   ← 累加          1 条
  add         Xk, Xk, #8               ← k+=svcntw()   1 条
  ptest       p0.s, P7, p0.s           ← 谓词测试      1 条
  b.any       loop                     ← 分支          1 条
──────────────────────────────────────────────────────
总计：10 条指令/迭代：6 条 SVE + 3 条标量 + 1 分支
```

**关键 instructions per iteration × iterations per (i,j)：**

| 指令类别 | 具体指令 | 条/迭代 | ×64 = 条/(i,j) |
|---------|---------|:------:|:-------------:|
| 加载 A/B 索引 | `ld1b` | 2 | 128 |
| 索引计算 | `lsl` + `orr` | 2 | 128 |
| **Gather 查表** | **`ld1w` (gather)** | **1** | **64** |
| 累加 | `fadd` | 1 | 64 |
| 循环控制 | `whilelt` + `ptest` + `b.any` | 3 | 192 |

#### 4.2.3 FP8 SVE 优化版（`matmul_fp8_lookup_sve_optimized`）

将 `lsl` 移出循环（预处理 A_shifted），每迭代省 1 条指令：

```
内循环体（SVE 256-bit）：
  whilelt     p0.s, Xk, XL             ← 谓词生成      1 条
  ld1w        Z0.s, p0/z, [XrowA_sh, Xk]  ← 加载预左移 A  1 条  ← 替代 ld1b+lsl
  ld1b        Z1.s, p0/z, [XrowB, Xk]  ← 加载 B 索引   1 条
  orr         Z1.s, p0/m, Z0.s, Z1.s   ← 拼接索引      1 条  ← 省去 lsl
  ld1w        Z2.s, p0/z, [Xtable, Z1.s, uxtw #2]  ← gather      1 条
  fadd        Z3.s, p0/m, Z3.s, Z2.s   ← 累加          1 条
  add         Xk, Xk, #8               ← k步进         1 条
  ptest       p0.s, P7, p0.s           ← 谓词测试      1 条
  b.any       loop                     ← 分支          1 条
──────────────────────────────────────────────────────
总计：9 条指令/迭代：5 条 SVE + 3 条标量 + 1 分支
```

**优化效果：** 比未优化版每迭代省 1 条 `lsl`，动态节省 64 × 41,984 × 10000 = **26.9G 条指令**。

#### 4.2.4 FP16 SVE 乘加（`matmul_sve_fp16`）

每 (i,j) 迭代 32 次，每次处理 16 个 fp16 元素：

```
内循环体（SVE 256-bit）：
  whilelt     p0.h, Xk, XL             ← 谓词生成      1 条
  ld1h        Z0.h, p0/z, [XrowA, Xk]  ← 加载 A[k]     1 条  ← svld1_f16
  ld1h        Z1.h, p0/z, [XrowB, Xk]  ← 加载 B[k]     1 条
  fmla        Z2.h, p0/m, Z0.h, Z1.h   ← 乘加融合      1 条  ← 1 条 = 16 次 MAC
  add         Xk, Xk, #16              ← k+=svcnth()   1 条
  ptest       p0.h, P7, p0.h           ← 谓词测试      1 条
  b.any       loop                     ← 分支          1 条
──────────────────────────────────────────────────────
总计：7 条指令/迭代：4 条 SVE + 2 条标量 + 1 分支
```

**关键对比：** FP16 每迭代仅需 7 条指令，而 FP8 SVE 需要 10 条（+43%）。

#### 4.2.5 INT8 SVE 点积（`matmul_int8_sve`）

每 (i,j) 迭代 16 次，每次处理 32 个 int8 元素：

```
内循环体（SVE 256-bit）：
  whilelt     p0.b, Xk, XL             ← 谓词生成      1 条
  ld1b        Z0.b, p0/z, [XrowA, Xk]  ← 加载 A[k]     1 条  ← svld1_s8
  ld1b        Z1.b, p0/z, [XrowB, Xk]  ← 加载 B[k]     1 条
  sdot        Z2.s, Z0.b, Z1.b          ← 32 元素点积   1 条  ← 1 条 = 32×32 点积
  add         Xk, Xk, #32              ← k+=svcntb()   1 条
  ptest       p0.b, P7, p0.b           ← 谓词测试      1 条
  b.any       loop                     ← 分支          1 条
──────────────────────────────────────────────────────
总计：7 条指令/迭代：4 条 SVE + 2 条标量 + 1 分支
```

#### 4.2.6 INT8 I8MM 矩阵乘（`matmul_int8_i8mm_complete`）

每 (i,j) 迭代 4 次，每次处理 4×32=128 个 int8 元素（2 行 × 2 列 × 32 列）：

```
内循环体（SVE I8MM 256-bit）：
  whilelt     p0.b, Xk, XL             ← 谓词生成      1 条
  ld1b        Z0.b, p0/z, [rA0+k]      ← 加载 A0[k]    1 条
  ld1b        Z1.b, p0/z, [rA1+k]      ← 加载 A1[k]    1 条
  ld1b        Z2.b, p0/z, [rB0+k]      ← 加载 B0[k]    1 条
  ld1b        Z3.b, p0/z, [rB1+k]      ← 加载 B1[k]    1 条
  smmla       acc00, Z0.b, Z2.b         ← A0×B0         1 条  ← 64 次 MAC
  smmla       acc01, Z0.b, Z3.b         ← A0×B1         1 条
  smmla       acc10, Z1.b, Z2.b         ← A1×B0         1 条
  smmla       acc11, Z1.b, Z3.b         ← A1×B1         1 条
  add         Xk, Xk, #32              ← k+=svcntb()   1 条
  ptest       p0.b, P7, p0.b           ← 谓词测试      1 条
  b.any       loop                     ← 分支          1 条
──────────────────────────────────────────────────────
总计：12 条指令/迭代：9 条 SVE + 2 条标量 + 1 分支
```

### 4.3 计数器与汇编指令的对应关系

| 计数器 | 对应的汇编指令 | 累加时机 |
|--------|-------------|---------|
| `loop_iters` | 内循环体入口 | 每次内循环迭代 +1 |
| `elem_processed` | 每一轮内循环处理的矩阵元素 | 内循环退出后 +L（完整处理的 K 维） |
| `reduce_count` | `faddv` / `saddv` 归约指令 | 内循环退出后 +1（每 (i,j) 一次归约） |
| `gather_count` | `ld1w` with `uxtw #2`（SVE gather） | 每次 gather 执行后 +1 |
| `compute_count` | `fmla` / `sdot` / `smmmla` | 每次计算指令执行后 +1（I8MM +4） |

### 4.4 从插桩计数推导动态指令总数

已知每迭代的静态指令数（来自 objdump），结合 `loop_iters` 即可推算各类动态指令数：

```python
# 通用公式
动态指令_类别 = loop_iters × 该类静态指令数/迭代

# 示例：FP8 SVE 各类动态指令数
total_loop_iters = 26,869,760,000  # 实测

load_A_B       = total_loop_iters × 2  = 53,739,520,000  # ld1b ×2
index_calc     = total_loop_iters × 2  = 53,739,520,000  # lsl + orr
gather         = total_loop_iters × 1  = 26,869,760,000  # ld1w gather ← 与 gather_count 一致 ✓
accumulate     = total_loop_iters × 1  = 26,869,760,000  # fadd
loop_ctrl_sve  = total_loop_iters × 1  = 26,869,760,000  # whilelt
loop_ctrl_scalar = total_loop_iters × 2 = 53,739,520,000 # add k + ptest + b.any

# 归约指令（仅每 (i,j) 执行一次）
reduce = 419,840,000  # faddv  ← 与 reduce_count 一致 ✓

# 函数框架指令（prologue/epilogue，每函数调用执行一次）
frame = 框架静态指令数 × 调用次数
```

### 4.5 标量指令与 SVE 指令的关系

`matmul_fp8_lookup_sve` 的静态指令分布：

```
总静态指令 168 = 120 条标量 + 11 条 SVE + 19 条分支 + 18 条其他
                (71%)        (7%)        (11%)       (11%)
```

虽然 SVE 指令只占 7%，但热路径中 SVE 指令被循环放大：

- 热路径 SVE（6 条/迭代 × 64 迭代 × 41,984 (i,j) = **16,121,856 条/(i,j)**）
- 热路径标量（3 条/迭代 × 同上 = **8,060,928 条/(i,j)**）
- 标量循环控制（3 条/迭代 × 同上 = **8,060,928 条/(i,j)**）

标量指令在热路径中与 SVE 指令**同比例放大**，71% 的静态占比意味着每执行 1 条 SVE 指令，同时有 ~2.5 条标量指令在运行。

### 4.6 向量化原理与迭代次数公式

```
向量化加速比 = 向量宽度 / 标量每次处理量
             = 标量迭代次数 / 向量化迭代次数

FP8 scalar vs FP8 SVE:
  加速比 = 512 / 64 = 8×  ← 与 svcntw() 一致

FP8 SVE vs FP16 SVE:
  加速比 = 64 / 32 = 2×  ← FP16 向量宽度是 FP8 的 2 倍
```

迭代次数与向量宽度的反比关系是性能差异的**数学根源**——相同的 L=512，向量宽度越宽，迭代次数越少，循环控制开销和访存指令数都成比例减少。

### 4.7 数据校验方法

动态计数可通过以下公式交叉验证：

```c
// 核心不变量
elem_processed ≡ L × N × S × loopCnt
reduce_count   ≡ N × S × loopCnt
loop_iters × vector_width ≈ elem_processed  (忽略尾部)

// 对 FP8 SVE:
64 iter/(i,j) × 8 元素/iter = 512 元素/(i,j) = L ✓

// 对 FP16 SVE:
32 iter/(i,j) × 16 元素/iter = 512 元素/(i,j) = L ✓
```

## 5. 实验结果

### 4.1 原始计数（总计 loopCnt=10000 次）

```
  Kernel                                   loop_iters       elem   reduce   gather  compute  iter/ij
  ─────────────────────────────────────────────────────────────────────────────────────────
  matmul_fp8_lookup_scalar                 214958080000 214958080000 419840000        0        0      512
  matmul_fp8_lookup_sve                    26869760000 214958080000 419840000 26869760000        0       64
  matmul_fp8_lookup_sve_optimized          26869760000 214958080000 419840000 26869760000        0       64
  matmul_sve_fp16                          13434880000 214958080000 419840000        0 13434880000       32
  matmul_int8_sve                           6717440000 214958080000 419840000        0  6717440000       16
  matmul_int8_i8mm_complete                 1679360000 214958080000 419840000        0  6717440000        4
```

### 4.2 每 (i,j) 对平均

```
  Kernel                                   loop_iters       elem   reduce   gather  compute
  ───────────────────────────────────────────────────────────────────────
  matmul_fp8_lookup_scalar                        512        512        1        0        0
  matmul_fp8_lookup_sve                             64        512        1       64        0
  matmul_fp8_lookup_sve_optimized                   64        512        1       64        0
  matmul_sve_fp16                                   32        512        1        0       32
  matmul_int8_sve                                   16        512        1        0       16
  matmul_int8_i8mm_complete                          4        512        1        0       16
```

### 4.3 基础校验

所有 kernel 的一致指标说明插桩正确：
- **总元素处理** = 128 × 328 × 512 × 10000 = **214,958,080,000** ✓
- **总归约次数** = 128 × 328 × 10000 = **419,840,000** ✓

## 6. 数据分析

### 6.1 向量化效率

| Kernel | 向量宽度 | iter/(i,j) | 元素/迭代 | SVE 指令/迭代 |
|--------|:-------:|:---------:|:--------:|:------------:|
| FP8 scalar | 1 (标量) | **512** | 1 | 0 |
| FP8 SVE | `svcntw()=8` | **64** | 8 | 6 |
| FP8 SVE opt | `svcntw()=8` | **64** | 8 | 5 |
| FP16 SVE | `svcnth()=16` | **32** | 16 | 4 |
| INT8 SVE | `svcntb()=32` | **16** | 32 | 4 |
| INT8 I8MM | 2×2×svcntb()=128 | **4** | 128 | 4 smmla/iter |

**关键发现：** FP8 查表受限于 32 位索引（`svld1ub_u32` 送入 32 位寄存器），只能使用 `svcntw()=8` 的向量宽度。FP16 使用 `svcnth()=16`，INT8 使用 `svcntb()=32`。FP8 的迭代次数是 FP16 的 **2×**，是 INT8 SVE 的 **4×**，是 I8MM 的 **16×**。

### 6.2 关键指令对比（每 (i,j) 对）

| 指令类别 | FP8 SVE | FP16 SVE | INT8 SVE | INT8 I8MM |
|---------|:-------:|:--------:|:--------:|:---------:|
| 加载 A 矩阵 | 128 (ld1ub_u32 ×2) | 32 (ld1_f16 ×2) | 32 (ld1_s8 ×2) | 32 (ld1_s8 ×2) |
| 索引计算 (lsl+orr) | 128 | 0 | 0 | 0 |
| **Gather 查表** | **64** ⚠️ | **0** | **0** | **0** |
| 计算指令 | 0 (仅有 fadd) | **32 fmla** | **16 sdot** | **16 smmla** |
| 循环控制 | 192 (×3) | 96 (×3) | 48 (×3) | 12 (×3) |
| **总 SVE 热路径指令** | **≥ 512** | **160** | **96** | **60** |

FP8 查表每 (i,j) 对需要 **512 条+ SVE 指令**，远高于其他 kernel：
- FP16: 160 条（FP8 的 31%）
- INT8 SVE: 96 条（FP8 的 19%）
- INT8 I8MM: 60 条（FP8 的 12%）

### 6.3 Gather vs 顺序 Load 的延迟差异

```
FP8 SVE gather 路径（每迭代，延迟叠加）：
  whilelt     1 cyc
  ld1ub_u32  2 cyc  ← A 索引（顺序 L1 hit）
  ld1ub_u32  2 cyc  ← B 索引（顺序 L1 hit）
  lsl        1 cyc  ← 索引计算
  orr        1 cyc  ← 索引拼接
  ld1w gather ~12 cyc  ← ⚠️ 随机查表 L1-miss → L2
  fadd       2 cyc  ← 累加
  ─────────────────
  sum        ~21 cyc / 迭代 × 64 = ~1344 cyc / (i,j)

FP16 SVE fmla 路径（每迭代）：
  whilelt     1 cyc
  ld1_f16    2 cyc  ← 顺序 A（L1 hit）
  ld1_f16    2 cyc  ← 顺序 B（L1 hit）
  fmla       3 cyc  ← 乘加融合
  ─────────────────
  sum        ~8 cyc / 迭代 × 32 = ~256 cyc / (i,j)
```

**FP8 查表的 gather 指令占每 (i,j) 总延迟的 57%**（64 × 12 / 1344），是绝对的瓶颈。

### 6.4 标量指令占比的隐藏成本

FP8 SVE 的内循环每迭代执行 6 条 SVE 指令（含 gather），但外围循环控制和地址计算还需要大量标量指令。从静态分析数据可知，`matmul_fp8_lookup_sve` 共有 **168 条静态指令**，其中 **120 条是标量指令（71%）**，仅 11 条是 SVE 指令。这意味着每次 SVE 迭代外围都有大量标量地址计算。

优化版将左移移出循环（预处理 A_shifted），静态指令从 168→96，标量指令占比从 71%→72%（但绝对值从 120→69）。

### 6.5 与理论值对比验证

| 指标 | FP8 scalar | FP8 SVE | FP16 SVE | INT8 SVE | INT8 I8MM |
|------|:---------:|:-------:|:--------:|:--------:|:---------:|
| iter/(i,j) 实测 | 512 | 64 | 32 | 16 | 4 |
| iter/(i,j) 理论 | 512 | 64 | 32 | 16 | 4 |
| 符合预期 | ✓ | ✓ | ✓ | ✓ | ✓ |

理论计算的迭代次数与实测完全一致，验证了静态分析和插桩的正确性。

### 6.6 访存与计算指令量化对比分析

#### 6.6.1 动态指令数分类统计（每函数调用）

基于插桩数据的 `loop_iters` × 静态指令数/迭代，推算单次 `matmul(A, B_T, C, 128, 328, 512)` 调用的各类指令动态总数：

**FP8 标量查表：**
```
指令类别             动态数            占比     推算方法
─────────────────────────────────────────────────────────
load A              21,495,808        8.7%   512 iter/(i,j) × 41,984 (i,j) × 1 ldrb
load B              21,495,808        8.7%   同上
load table          21,495,808        8.7%   512 × 41,984 × 1 ldr
索引计算(lsl+orr)   42,991,616       17.4%   512 × 41,984 × 2
累加(fadd)          21,495,808        8.7%   512 × 41,984 × 1
循环控制(cmp+b)     42,991,616       17.4%   512 × 41,984 × 2
归约(saddv)(标量)       41,984        0.0%   41,984 × 1
─────────────────────────────────────────────────────────
总计                 ~247M          100%
```

**FP8 SVE 查表：**
```
指令类别             动态数            占比     推算方法
─────────────────────────────────────────────────────────
load A (ld1b)        2,686,976        4.9%   64 iter/(i,j) × 41,984 (i,j) × 1
load B (ld1b)        2,686,976        4.9%   同上
索引计算(lsl+orr)    5,373,952        9.8%   64 × 41,984 × 2
gather 查表(ld1w)    2,686,976        4.9%   64 × 41,984 × 1  ← ⚠️ 高延迟
累加(fadd)           2,686,976        4.9%   64 × 41,984 × 1
循环控制(SVE)        2,686,976        4.9%   64 × 41,984 × 1 whilelt
循环控制(标量)       5,373,952        9.8%   64 × 41,984 × 2 add+ptest+b
归约(faddv)             41,984        0.1%   41,984 × 1
─────────────────────────────────────────────────────────
SVE 热路径小计        21,495,808      39.0%
+ 标量地址计算        33,587,200      61.0%
─────────────────────────────────────────────────────────
总计                 ~55M            100%
```

> 标量地址计算：每 (i,j) 对的 i/j 循环控制、指针偏移、外层赋值等。来自静态 120 条标量指令中非热路径部分的动态计数。

**FP16 SVE 乘加：**
```
指令类别             动态数            占比
─────────────────────────────────────────────────────────
load A (ld1h)        1,343,488        6.7%   32 iter × 41,984 × 1
load B (ld1h)        1,343,488        6.7%   同上
fmla 乘加融合        1,343,488        6.7%   32 × 41,984 × 1  ← 每条 = 16 MAC
循环控制(SVE)        1,343,488        6.7%   whilelt
循环控制(标量)       2,686,976       13.3%   add+ptest+b
归约(faddv)             41,984        0.2%
─────────────────────────────────────────────────────────
SVE 热路径小计         8,060,928      40.0%
+ 标量地址计算        12,095,232      60.0%
─────────────────────────────────────────────────────────
总计                 ~20M            100%
```

**INT8 SVE 点积：**
```
指令类别             动态数            占比
─────────────────────────────────────────────────────────
load A (ld1b)          671,744        4.0%   16 iter × 41,984 × 1
load B (ld1b)          671,744        4.0%   同上
sdot 点积              671,744        4.0%   16 × 41,984 × 1  ← 每条 = 32×32 点积
循环控制(SVE)          671,744        4.0%   whilelt
循环控制(标量)        1,343,488        8.0%   add+ptest+b
归约(saddv)             41,984        0.3%
─────────────────────────────────────────────────────────
SVE 热路径小计         4,031,488      24.0%
+ 标量地址计算         6,047,232      76.0%
─────────────────────────────────────────────────────────
总计                 ~17M            100%
```

**INT8 I8MM 矩阵乘：**
```
指令类别             动态数            占比
─────────────────────────────────────────────────────────
load A (ld1b)          335,872        3.2%   8 iter × (128/2)(i) × (328/2)(j)
load B (ld1b)          335,872        3.2%   同上
smmmla 矩阵乘          671,744        6.4%   ×4 smmla/iter  ← 每条 = 256 MAC
循环控制(SVE)          167,936        1.6%   whilelt
循环控制(标量)         335,872        3.2%   add+ptest+b
归约(saddv)             41,984        0.4%
─────────────────────────────────────────────────────────
SVE 热路径小计         1,889,280      18.0%
+ 标量地址计算         2,831,616      82.0%
─────────────────────────────────────────────────────────
总计                 ~10.5M           100%
```

#### 6.6.2 加载指令对比汇总

| Kernel | load A 次数 | load B 次数 | table load 次数 | 总访存指令 | 访存类型 |
|--------|:---------:|:---------:|:-------------:|:---------:|:--------:|
| FP8 scalar | 21.5M | 21.5M | 21.5M (ldr) | **64.5M** | 标量顺序 |
| **FP8 SVE** | **2.7M** | **2.7M** | **2.7M (gather)** | **8.1M** | SVE 顺序 + ⚠️ **随机** |
| FP8 SVE opt | 2.7M | 2.7M | 2.7M (gather) | 8.1M | 同上 |
| **FP16 SVE** | **1.3M** | **1.3M** | **0** | **2.7M** | SVE 顺序 |
| INT8 SVE | 0.67M | 0.67M | 0 | 1.3M | SVE 顺序 |
| INT8 I8MM | 0.34M | 0.34M | 0 | 0.68M | SVE 顺序 |

**观察：**
- FP8 SVE 的总访存指令是 FP16 的 **3×**（8.1M vs 2.7M）
- FP8 SVE 的总访存指令是 I8MM 的 **12×**（8.1M vs 0.68M）
- 更关键的是：FP8 的 table load 是**随机 gather**（~12 cyc），其他所有访存都是**顺序 L1 hit**（~2 cyc）

#### 6.6.3 计算指令对比汇总

| Kernel | 计算指令 | 条/call | 每指令操作数 | 总 MAC 操作 |
|--------|---------|:------:|:----------:|:----------:|
| FP8 SVE | fadd | 2.7M | 8 次加法 | 2.7M ⊕ (非乘积) |
| FP16 SVE | **fmla** | **1.3M** | **16 MAC** | **21.5M** ⨯ |
| INT8 SVE | **sdot** | **0.67M** | **32 MAC** | **21.5M** ⨯ |
| INT8 I8MM | **smmmla** | **0.67M** | **256 MAC** | **171.8M** ⨯ |

总 MAC 操作计算公式：
```
FP16:  1,343,488 fmla × 16 MAC/fmla  = 21,495,808  MAC
INT8:    671,744 sdot × 32 MAC/sdot   = 21,495,808  MAC
I8MM:    671,744 smmla × 256 MAC/smmmla = 171,859,968 MAC
```

**关键发现：** FP16 和 INT8 SVE 总 MAC 数相同（21.5M），但 I8MM 通过 2×2 分块将计算密度提升 **8×**（171.8M MAC）。FP8 查表的 `fadd` 只做加法不做乘法，严格来说不是 MAC 操作，这也是查表法的本质限制——结果已预计算在表中，只需累加。

#### 6.6.4 周期估算与时间分析

采用典型鲲鹏 920 指令延迟（流水线理想情况，未考虑数据相关停顿）：

| 指令类型 | 延迟(cyc) | 说明 |
|---------|:--------:|------|
| 顺序 ld1 SVE (L1 hit) | 2 | 连续地址，预取命中 |
| Gather ld1w (L2 hit) | **12** | 随机地址，L1-miss |
| 标量 ldr (L1 hit) | 2 | |
| fmla (fp16) | 3 | 流水线友好 |
| sdot (int8) | 3 | 流水线友好 |
| smmla (int8) | 4 | 多周期 |
| fadd | 2 | |
| lsl/orr/add (标量) | 1 | |
| whilelt/ptest | 1 | |
| b.any | 1 | 预测成功 |

**FP8 SVE 查表周期分解（每函数调用）：**

```
指令类别          次数(call)  × 延迟 =  周期      占比
──────────────────────────────────────────────────────
load A (ld1b)      2,686,976     2      5,373,952    3.7%
load B (ld1b)      2,686,976     2      5,373,952    3.7%
lsl + orr          5,373,952     1      5,373,952    3.7%
gather (ld1w)      2,686,976    12     32,243,712   22.0%  ← ⚠️ 瓶颈
fadd               2,686,976     2      5,373,952    3.7%
whilelt            2,686,976     1      2,686,976    1.8%
add + ptest + b    5,373,952     1      5,373,952    3.7%
──────────────────────────────────────────────────────
热路径 SVE 合计                             62,000,448   42.3%
标量地址计算              ~33.6M       1     33,587,200   22.9%
归约 faddv                 41,984     5        209,920    0.1%
──────────────────────────────────────────────────────
总估算周期                             ~95,797,568    100%
```

**FP16 SVE 乘加周期分解（每函数调用）：**

```
指令类别          次数(call)  × 延迟 =  周期      占比
──────────────────────────────────────────────────────
load A (ld1h)      1,343,488     2      2,686,976    5.0%
load B (ld1h)      1,343,488     2      2,686,976    5.0%
fmla               1,343,488     3      4,030,464    7.5%
whilelt            1,343,488     1      1,343,488    2.5%
add + ptest + b    2,686,976     1      2,686,976    5.0%
──────────────────────────────────────────────────────
热路径 SVE 合计                             13,434,880   25.0%
标量地址计算              ~12.1M       1     12,095,232   22.5%
归约 faddv                 41,984     5        209,920    0.4%
──────────────────────────────────────────────────────
总估算周期                             ~25,740,032    100%
```

**INT8 SVE 点积周期分解（每函数调用）：**

```
指令类别          次数(call)  × 延迟 =  周期      占比
──────────────────────────────────────────────────────
load A (ld1b)        671,744     2      1,343,488    5.4%
load B (ld1b)        671,744     2      1,343,488    5.4%
sdot                 671,744     3      2,015,232    8.1%
whilelt              671,744     1        671,744    2.7%
add + ptest + b    1,343,488     1      1,343,488    5.4%
──────────────────────────────────────────────────────
热路径 SVE 合计                              6,717,440   27.0%
标量地址计算               ~6.0M       1      6,047,232   24.3%
归约 saddv                 41,984     5        209,920    0.8%
──────────────────────────────────────────────────────
总估算周期                             ~12,974,592    100%
```

**INT8 I8MM 周期分解（每函数调用）：**

```
指令类别          次数(call)  × 延迟 =  周期      占比
──────────────────────────────────────────────────────
load A (ld1b)        335,872     2        671,744    6.8%
load B (ld1b)        335,872     2        671,744    6.8%
smmmla ×4            671,744     4      2,686,976   27.3%
whilelt              167,936     1        167,936    1.7%
add + ptest + b      335,872     1        335,872    3.4%
──────────────────────────────────────────────────────
热路径 SVE 合计                              4,534,272   46.1%
标量地址计算               ~2.8M       1      2,831,616   28.8%
归约 saddv                 41,984     5        209,920    2.1%
──────────────────────────────────────────────────────
总估算周期                             ~9,842,688    100%
```

#### 6.6.5 指令延迟与性能对比总结

| 指标 | FP8 SVE | FP16 SVE | INT8 SVE | INT8 I8MM |
|------|:-------:|:--------:|:--------:|:---------:|
| 总访存指令 | 8.1M | 2.7M | 1.3M | 0.68M |
| 其中 gather 数 | **2.7M** | 0 | 0 | 0 |
| 总计算指令 | 2.7M (fadd) | 1.3M (fmla) | 0.67M (sdot) | 0.67M (smmmla) |
| 总 MAC 操作 | (无乘法) | **21.5M** | **21.5M** | **171.8M** |
| 估算总周期 | **95.8M** | **25.7M** | **13.0M** | **9.8M** |
| 相对 FP16 | **3.7×** | 1.0× | **0.51×** | **0.38×** |
| gather 周期占比 | **22%** | 0% | 0% | 0% |
| 加载 vs 计算周期比 | 1.2:1 | **1.3:1** | **0.7:1** | **0.5:1** |

**关键结论：**

1. **FP8 SVE 总周期是 FP16 的 3.7×**，其中 gather 独占 22% 的周期
2. **FP8 的总访存指令数是 FP16 的 3×**，但更大的问题是访存类型——gather 的延迟是顺序 load 的 6×
3. **FP16 的加载周期比计算周期多**（1.3:1），说明 FP16 是访存受限，而非计算受限
4. **I8MM 的计算占比远高于访存**（加载:计算 = 0.5:1），更接近计算密集型，硬件利用率更高
5. **FP8 没有真正的乘-累加**——查表法将乘法替换为查表，只做加法累加。这虽然避免了乘法操作，但查表的代价（gather 周期 + 额外索引计算）远超直接乘法的代价

### 7.1 核心瓶颈排序

FP8 查表法慢于 FP16/INT8 的原因，按影响程度排序：

1. **向量宽度受限（主因）**：`svld1ub_u32` 使用 32 位通道，仅 8 元素/迭代，是 FP16（16 元素）的 1/2，INT8（32 元素）的 1/4
2. **随机 gather 访存（次因）**：每次 gather ~12 cyc L2 延迟，64 次 gather/(i,j) 占热路径周期的 57%
3. **额外索引计算**：lsl + orr 每迭代 2 条额外指令，增加 33% 的 SVE 指令数
4. **高迭代次数放大循环控制开销**：64 次迭代 × 3 条控制指令 = 192 条/(i,j)，是 I8MM 的 16 倍

### 7.2 量化对比

| 指标 | FP8 SVE | FP16 SVE | INT8 SVE | INT8 I8MM |
|------|:-------:|:--------:|:--------:|:---------:|
| 总热路径 SVE 指令/(i,j) | 512 | 160 | 96 | 60 |
| gather 指令/(i,j) | 64 | 0 | 0 | 0 |
| 标量指令/(i,j) | ~960 | ~480 | ~144 | ~48 |
| 估算周期/(i,j) | ~1,344 | ~256 | ~144 | ~120 |
| 相对 FP16 | **5.3×** | 1.0× | 0.56× | 0.47× |

### 7.3 优化方向

1. **降低 gather 延迟**：将 256 KiB LUT 拆分到 L1 可容纳的子表（实验 6 思路），使 gather 从 L2 hit 变为 L1 hit
2. **减少迭代次数**：使用更高向量宽度的数据类型（如 bf16 替代 u32 索引）
3. **减少标量开销**：循环展开、减少地址重计算、预处理更多计算到内存之外
