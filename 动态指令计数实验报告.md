# main.cpp 动态指令计数实验报告

## 1. 实验目的

对 main.cpp 中 6 个矩阵乘法核函数，通过代码插桩精确测量运行时动态指令执行次数，按功能分类量化分析各 kernel 的指令构成差异，从指令级解释 FP8 查表法为何慢于 FP16/INT8 直接乘法。

## 2. 测试环境

| 项目 | 配置 |
|------|------|
| CPU | 鲲鹏 920 (Kunpeng 920) |
| SVE 向量宽度 | 256-bit |
| svcntw() / svcnth() / svcntb() | 8 / 16 / 32 (×128 = 256-bit) |
| L1d Cache | 64 KiB |
| L2 Cache | 1280 KiB |
| 编译选项 | `-O3 -march=armv8.2-a+sve+fp16+i8mm` |
| 矩阵尺寸 | N=128, S=328, L=512 |
| 总 (i,j) 对数 | 128 × 328 = 41,984 |
| 循环次数 (loopCnt) | 10,000 |

## 3. 测试方法：代码插桩（方案 C）

在每个核函数的热循环中插入轻量级计数器，统计以下指标：

| 计数器 | 含义 |
|--------|------|
| `loop_iters` | 最内层热循环迭代次数 |
| `elem_processed` | 处理的矩阵元素总数（= L × (i,j) 对数） |
| `reduce_count` | 向量归约（faddv/saddv）执行次数 |
| `gather_count` | SVE gather 查表指令数（FP8 专用） |
| `compute_count` | 核心计算指令数（fmla / sdot / smmla） |

计数器使用 `uint64_t` 累加，覆盖 loopCnt × N × S 次执行，不改变编译器优化行为。

## 4. 实验结果

### 4.1 原始计数（总计 loopCnt=10000 次）

```
  Kernel                                   loop_iters       elem   reduce   gather  compute  iter/ij
  ─────────────────────────────────────────────────────────────────────────────────────────
  matmul_fp8_lookup_scalar                 214958080000 214958080000 419840000        0        0      512
  matmul_fp8_lookup_sve                    26869760000 214958080000 419840000 26869760000        0       64
  matmul_fp8_lookup_sve_optimized          26869760000 214958080000 419840000 26869760000        0       64
  matmul_sve_fp16                          13434880000 214958080000 419840000        0 13434880000       32
  matmul_int8_sve                           6717440000 214958080000 419840000        0  6717440000       16
  matmul_int8_i8mm_complete                 1679360000 214958080000 419840000        0  6717440000        4
```

### 4.2 每 (i,j) 对平均

```
  Kernel                                   loop_iters       elem   reduce   gather  compute
  ───────────────────────────────────────────────────────────────────────
  matmul_fp8_lookup_scalar                        512        512        1        0        0
  matmul_fp8_lookup_sve                             64        512        1       64        0
  matmul_fp8_lookup_sve_optimized                   64        512        1       64        0
  matmul_sve_fp16                                   32        512        1        0       32
  matmul_int8_sve                                   16        512        1        0       16
  matmul_int8_i8mm_complete                          4        512        1        0       16
```

### 4.3 基础校验

所有 kernel 的一致指标说明插桩正确：
- **总元素处理** = 128 × 328 × 512 × 10000 = **214,958,080,000** ✓
- **总归约次数** = 128 × 328 × 10000 = **419,840,000** ✓

## 5. 数据分析

### 5.1 向量化效率

| Kernel | 向量宽度 | iter/(i,j) | 元素/迭代 | SVE 指令/迭代 |
|--------|:-------:|:---------:|:--------:|:------------:|
| FP8 scalar | 1 (标量) | **512** | 1 | 0 |
| FP8 SVE | `svcntw()=8` | **64** | 8 | 6 |
| FP8 SVE opt | `svcntw()=8` | **64** | 8 | 5 |
| FP16 SVE | `svcnth()=16` | **32** | 16 | 4 |
| INT8 SVE | `svcntb()=32` | **16** | 32 | 4 |
| INT8 I8MM | 2×2×svcntb()=128 | **4** | 128 | 4 smmla/iter |

**关键发现：** FP8 查表受限于 32 位索引（`svld1ub_u32` 送入 32 位寄存器），只能使用 `svcntw()=8` 的向量宽度。FP16 使用 `svcnth()=16`，INT8 使用 `svcntb()=32`。FP8 的迭代次数是 FP16 的 **2×**，是 INT8 SVE 的 **4×**，是 I8MM 的 **16×**。

### 5.2 关键指令对比（每 (i,j) 对）

| 指令类别 | FP8 SVE | FP16 SVE | INT8 SVE | INT8 I8MM |
|---------|:-------:|:--------:|:--------:|:---------:|
| 加载 A 矩阵 | 128 (ld1ub_u32 ×2) | 32 (ld1_f16 ×2) | 32 (ld1_s8 ×2) | 32 (ld1_s8 ×2) |
| 索引计算 (lsl+orr) | 128 | 0 | 0 | 0 |
| **Gather 查表** | **64** ⚠️ | **0** | **0** | **0** |
| 计算指令 | 0 (仅有 fadd) | **32 fmla** | **16 sdot** | **16 smmla** |
| 循环控制 | 192 (×3) | 96 (×3) | 48 (×3) | 12 (×3) |
| **总 SVE 热路径指令** | **≥ 512** | **160** | **96** | **60** |

FP8 查表每 (i,j) 对需要 **512 条+ SVE 指令**，远高于其他 kernel：
- FP16: 160 条（FP8 的 31%）
- INT8 SVE: 96 条（FP8 的 19%）
- INT8 I8MM: 60 条（FP8 的 12%）

### 5.3 Gather vs 顺序 Load 的延迟差异

```
FP8 SVE gather 路径（每迭代，延迟叠加）：
  whilelt     1 cyc
  ld1ub_u32  2 cyc  ← A 索引（顺序 L1 hit）
  ld1ub_u32  2 cyc  ← B 索引（顺序 L1 hit）
  lsl        1 cyc  ← 索引计算
  orr        1 cyc  ← 索引拼接
  ld1w gather ~12 cyc  ← ⚠️ 随机查表 L1-miss → L2
  fadd       2 cyc  ← 累加
  ─────────────────
  sum        ~21 cyc / 迭代 × 64 = ~1344 cyc / (i,j)

FP16 SVE fmla 路径（每迭代）：
  whilelt     1 cyc
  ld1_f16    2 cyc  ← 顺序 A（L1 hit）
  ld1_f16    2 cyc  ← 顺序 B（L1 hit）
  fmla       3 cyc  ← 乘加融合
  ─────────────────
  sum        ~8 cyc / 迭代 × 32 = ~256 cyc / (i,j)
```

**FP8 查表的 gather 指令占每 (i,j) 总延迟的 57%**（64 × 12 / 1344），是绝对的瓶颈。

### 5.4 标量指令占比的隐藏成本

FP8 SVE 的内循环每迭代执行 6 条 SVE 指令（含 gather），但外围循环控制和地址计算还需要大量标量指令。从静态分析数据可知，`matmul_fp8_lookup_sve` 共有 **168 条静态指令**，其中 **120 条是标量指令（71%）**，仅 11 条是 SVE 指令。这意味着每次 SVE 迭代外围都有大量标量地址计算。

优化版将左移移出循环（预处理 A_shifted），静态指令从 168→96，标量指令占比从 71%→72%（但绝对值从 120→69）。

### 5.5 与理论值对比验证

| 指标 | FP8 scalar | FP8 SVE | FP16 SVE | INT8 SVE | INT8 I8MM |
|------|:---------:|:-------:|:--------:|:--------:|:---------:|
| iter/(i,j) 实测 | 512 | 64 | 32 | 16 | 4 |
| iter/(i,j) 理论 | 512 | 64 | 32 | 16 | 4 |
| 符合预期 | ✓ | ✓ | ✓ | ✓ | ✓ |

理论计算的迭代次数与实测完全一致，验证了静态分析和插桩的正确性。

## 6. 结论

### 6.1 核心瓶颈排序

FP8 查表法慢于 FP16/INT8 的原因，按影响程度排序：

1. **向量宽度受限（主因）**：`svld1ub_u32` 使用 32 位通道，仅 8 元素/迭代，是 FP16（16 元素）的 1/2，INT8（32 元素）的 1/4
2. **随机 gather 访存（次因）**：每次 gather ~12 cyc L2 延迟，64 次 gather/(i,j) 占热路径周期的 57%
3. **额外索引计算**：lsl + orr 每迭代 2 条额外指令，增加 33% 的 SVE 指令数
4. **高迭代次数放大循环控制开销**：64 次迭代 × 3 条控制指令 = 192 条/(i,j)，是 I8MM 的 16 倍

### 6.2 量化对比

| 指标 | FP8 SVE | FP16 SVE | INT8 SVE | INT8 I8MM |
|------|:-------:|:--------:|:--------:|:---------:|
| 总热路径 SVE 指令/(i,j) | 512 | 160 | 96 | 60 |
| gather 指令/(i,j) | 64 | 0 | 0 | 0 |
| 标量指令/(i,j) | ~960 | ~480 | ~144 | ~48 |
| 估算周期/(i,j) | ~1,344 | ~256 | ~144 | ~120 |
| 相对 FP16 | **5.3×** | 1.0× | 0.56× | 0.47× |

### 6.3 优化方向

1. **降低 gather 延迟**：将 256 KiB LUT 拆分到 L1 可容纳的子表（实验 6 思路），使 gather 从 L2 hit 变为 L1 hit
2. **减少迭代次数**：使用更高向量宽度的数据类型（如 bf16 替代 u32 索引）
3. **减少标量开销**：循环展开、减少地址重计算、预处理更多计算到内存之外
