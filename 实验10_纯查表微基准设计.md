# 实验10：纯查表微基准（预计算索引，无索引计算开销）

## 1. 背景与目的

### 1.1 问题

实验7 测量了标量相位（含索引计算 + BF16 转换 + 查表）和 SVE 相位（累加 + 归约），但无法区分**查表本身**和**索引计算**各自的开销。

### 1.2 目标

将索引对 `(a, b)` 预计算为 `indices[i]`，使得标量相位变成纯查表（无 `(a<<8)|b` 计算，无 BF16→float 转换），从而：

- 隔离出**纯查表延迟**
- 对比实验7的标量相位，推算出索引计算 + BF16 转换的开销

---

## 2. 测量方法

**计时方式**：`high_resolution_clock`（ns 精度），分 3 阶段计时。

| # | 阶段 | 伪代码 | 说明 |
|---|------|--------|------|
| 1 | 纯查表（标量） | `for i: local_vals[i] = sub[indices[i]]` | float 表直接读取，无索引计算 |
| 2 | SVE 累加 | `svld1_f32(local_vals) + svadd_f32_m` | 从 local_vals 连续加载累加 |
| 3 | SVE 归约 | `svaddv_f32(acc)` | 水平归约到标量 |

**关键区别**：
- 实验7标量相位：`idx = a_vals[i] * TABLE_B + b_vals[i]`（乘+加索引计算）+ BF16 查表 + `<< 16` 转换
- 实验10标量相位：`local_vals[i] = sub[indices[i]]`（纯查表，float 直读）

**对比实验7即可测得索引计算 + BF16 转换的开销**。

---

## 3. 实验设置

| 参数 | 值 |
|------|-----|
| 数据格式 | float（4B/entry） |
| 表维度扫描 | 2×256, 4×256, 8×256, 16×256, 32×256 |
| 查表次数 | `128 / (table_a / 2)`，随表变大而减少（匹配实验7） |
| 预热 | 2000 次 |
| 采样 | 20000 次 |
| 平台 | Kunpeng 920, 256-bit SVE |

---

## 4. 输出格式

```
表维度          表大小(KiB)    查表次数        平均耗时(μs)     总查表(ns)      每次查表(ns)    L1-miss%       标量(μs)        SVE累加(μs)     归约(μs)       标量vs实验7
--------------------------------------------------------------------------------------------------------------------------------------------------------------------
2×256           2              128             x.xxxx           xx.xx           xx.xx           x.xx%           x.xxxx           x.xxxx           x.xxxx           —
4×256           4              64              x.xxxx           xx.xx           xx.xx           x.xx%           x.xxxx           x.xxxx           x.xxxx           —
8×256           8              32              x.xxxx           xx.xx           xx.xx           x.xx%           x.xxxx           x.xxxx           x.xxxx           —
16×256          16             16              x.xxxx           xx.xx           xx.xx           x.xx%           x.xxxx           x.xxxx           x.xxxx           —
32×256          32             8               x.xxxx           xx.xx           xx.xx           x.xx%           x.xxxx           x.xxxx           x.xxxx           —
```

**列说明**：

| 列 | 含义 |
|----|------|
| 表维度 | `table_a × 256`，即子表的第一个维度 |
| 表大小(KiB) | entries × 4B / 1024 |
| 查表次数 | 每轮循环查表元素数 N |
| 平均耗时(μs) | 完整三阶段总耗时 / ITERS |
| 总查表(ns) | 平均耗时 × 1000 |
| 每次查表(ns) | 总查表 / 查表次数 |
| L1-miss% | L1D_CACHE_REFILL / L1D_CACHE |
| 标量(μs) | 阶段1（纯查表）平均耗时 |
| SVE累加(μs) | 阶段2（SVE 向量累加）平均耗时 |
| 归约(μs) | 阶段3（svaddv_f32）平均耗时 |
| 标量vs实验7 | 手动对比实验7同维度标量相位耗时 |

---

## 5. 预期分析

- **纯查表 ≈ 4-8 ns/次**（L1 hit），远低于实验7的标量相位（含索引计算 + BF16 转换）
- **表越大 → L1-miss 越高 → 标量相位越慢**
- SVE 累加和归约不随表大小变化（处理 local_vals 连续数组，总是 L1 hit）
- 实验10 标量相位耗时 ≈ 实验7 标量相位耗时 - 索引计算 - BF16 转换开销

---

## 6. 代码索引

| 符号 | 说明 |
|------|------|
| `exp10_pure_lookup_microbench()` | 主函数：表维度扫描 + 分阶段计时 |

### 编译运行

```bash
g++ -O3 -fopenmp -march=armv8.2-a+sve -o fp8_server matmul_fp8_server.cpp -std=c++17 -lnuma
./fp8_server 10
```
