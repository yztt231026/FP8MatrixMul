# 实验 6：L1 分组建表查表 — 详细实验报告

## 1. 实验背景

### 1.1 问题

FP8 查表法矩阵乘法的查找表（LUT）大小为 **256 KiB**，远超过鲲鹏 L1d **64 KiB/核**。每次查表需要访问 L2（~12 周期），若能放进 L1（~4 周期），延迟可降低 ~3×。

### 1.2 核心思路：切 LUT + 多核协作

将完整 LUT（256×256）按 idxA 范围切为 G 个子表，G 个核同时计算同一个输出元素 `(i,j)`，各自查找自己的子表，最后用 `#pragma omp atomic` 累加部分和。

```
原始查表（单核，全表 L2）：
  table[A[i][k]][B_T[j][k]]  → 每次查表 L2 ~12 cyc
  单核处理所有 512 个 k

分组建表（G 核，子表 L1）：
  核0: subtable_0[...]  核1: subtable_1[...]  ...
  → 每次查表 L1 ~4 cyc
  → 结果 = 部分和_0 + 部分和_1 + ... (atomic 累加)
```

### 1.3 float 子表

子表使用 **float**（4B/entry），与原始 LUT 格式一致，无需 BF16↔float 转换。

| G | step | 子表维度 | 子表大小 | L1 驻留 |
|---|------|---------|---------|---------|
| 1 | 256 | 256×256 | 256 KiB | ❌ L2 |
| 2 | 128 | 128×256 | 128 KiB | ❌ L2 |
| 4 | 64 | 64×256 | **64 KiB** | **L1 边界** |
| 8 | 32 | 32×256 | **32 KiB** | **✅ L1** |
| 16 | 16 | 16×256 | 16 KiB | ✅ L1 |
| 32 | 8 | 8×256 | 8 KiB | ✅ L1 |
| 64 | 4 | 4×256 | 4 KiB | ✅ L1 |

> G=4（64 KiB）处于 L1 边界，G=8 起完全驻留 L1。

---

## 2. 实验设置

### 2.1 固定条件

| 参数 | 值 |
|------|-----|
| 矩阵 | N=128, S=328, L=512 |
| 线程绑定 | 1 NUMA node, 80线程, `OMP_PLACES=cores OMP_PROC_BIND=close` |
| LUT 格式 | float（4B/entry，原始 LUT 直接切分，无 BF16 转换） |
| 基线 | `lookup_sve_omp()` — SVE + 80 核行并行，无分组 |
| 编译器 | `g++ -O3 -fopenmp -march=armv8.2-a+sve` |
| 平台 | HiSilicon Kunpeng, 256-bit SVE, L1d=64KiB, L2=1280KiB |
| 预热 | 100 次 |
| 采样 | **10000 次** |

### 2.2 测试变量

扫描 G ∈ {1, 2, 4, 8, 16, 32, 64}，观察子表大小、Atomic 竞争、负载均衡的综合影响。同时对比 BF16 子表（2B/entry）和 float 子表（4B/entry）两套方案。

### 2.3 预处理：按 idxA 分组

对每行 A，将 k 索引按 `g = A[i][k] / (256/G)` 分配到 G 组，组内连续存储。每组记录 `row_start[i*G+g]`（起始位置）和 `row_count[i*G+g]`（元素个数）。

![A矩阵重排前后对应关系](exp6_reorder_mapping.png)

重排的关键是 **k_indices 数组**：A_grouped 改变了元素的物理顺序（组内连续），但每个元素在原始行中的 k 索引被保存在 k_indices 的对应位置。运行时，核 g 用 `k_indices[t]` 回追原始的 k，从而正确地从 B_T 行中取出 b_val。

```
原始 A[i] 行：   A[i][0]=10,  A[i][1]=80,  A[i][2]=200,  A[i][3]=150, ...
                      ↓          ↓
                   idxA∈组0    idxA∈组1   ...

重排后：
A_grouped[]:   [组0元素...][组1元素...][组2元素...][组3元素...]
k_indices[]:   [各元素对应的原始 k 索引]

核 g 读取时：
  start = row_start[i*G+g]
  count = row_count[i*G+g]
  for t in 0..count-1:
    a_val = A_grouped[start + t]          // idxA 值
    k_orig = k_indices[start + t]         // 原始 k 位置
    b_val = B_T[j][k_orig]                // 通过原始 k 取 B_T
```

B_T 不需要重排，因为每个核都各自随机读 B_T 行（512B 在 L1 中），开销很小。

预处理数据结构：

```
A_grouped[total]:  重排后的 A 值（组内连续，存储原始 idxA）
k_indices[total]:  每个元素对应的原始 k（用于收集 B_T）
row_start/row_count: 每行每组的起始和计数
```

存储开销（G=64）：row_start/row_count ~64 KiB + A_grouped 64 KiB + k_indices 256 KiB ≈ **384 KiB**。

### 2.4 核任务分配原理

所有 G 个核同时计算**同一个输出元素 (i,j)**，但各自只处理 idxA 落在自己值域范围内的那部分 k 元素，查自己的子表，最后通过 atomic 累加合并部分和：

```
#pragma omp parallel num_threads(G)
{
    int g = omp_get_thread_num();          // 核 g
    float *sub = subtables[g];             // 持 float 子表 g
    int base = g * step;                   // 子表覆盖 [base, base+step)

    for (int i = 0; i < N; ++i)
        for (int j = 0; j < S; ++j) {
            // 核 g 只处理组 g 的 k 元素
            int cnt = row_count[i*G + g];
            // 阶段1：标量索引计算（B_T 收集 + 子表偏移）
            for (int t = 0; t < cnt; ++t) {
                int b_val = b_row[k_ptr[t]];
                idx_buf[t] = (a_ptr[t] - base) * 256u + b_val;
            }
            // 阶段2：SVE gather 查表 + 累加
            // svld1_gather_u32index_f32 + svadd_f32_m + svaddv_f32
            // #pragma omp atomic -> C[i][j] += part_sum
        }
}
```

对应的 4 面板示意图可以直观展示完整流程：

![实验6核任务分配](exp6_task_assignment.png)

| 面板 | 内容 |
|------|------|
| (a) | idxA 值域 [0,255] 均分 G 段，每段转为 BF16 子表（2B/entry），G≥2 起可驻留 L1 |
| (b) | 预处理：A 每行按 idxA 所属组重排为连续存储，记录 start/count |
| (c) | 运行时：G 个核全量遍历 (i,j)，各自查子表 + SVE 累加，atomic 合并到 C[i][j] |
| (d) | 计时方式与负载不均——min~max 反映各核计算时间差异 |

核心理解：与常规 OpenMP 行并行（每核分不同的行）不同，分组方案中 **所有核的循环范围完全一致**，区别仅在于内部处理的 k 元素子集不同。负载不均来自随机 idxA 分布导致各组的 `row_count[i*G+g]` 不相等。

---

## 3. 核函数：`lookup_sve_grouped_omp`

### 3.1 设计原理

使用 **float 子表**（4B/entry，与原始 LUT 格式一致），不再需要 BF16↔float 转换。采用 **标量索引计算 + SVE gather 查表** 的两阶段设计：

```
阶段1（标量）：合并 B_T 收集 + 索引计算
    for t in 0..count:
        b_val = b_row[k_ptr[t]]
        idx_buf[t] = (a_ptr[t] - base) * 256 + b_val   // 子表偏移

阶段2（SVE）：向量化 gather 查表 + 累加
    svld1_gather_u32index_f32  → 从 float 子表 gather 8 个元素
    svadd_f32_m                → 累加（merge 谓词，保留不活跃通道）
    svaddv_f32                → 归约
    #pragma omp atomic        → 合并到 C[i][j]
```

### 3.2 关键实现细节

```cpp
// 阶段1：标量索引计算（无查表、无 BF16 转换）
alignas(64) uint32_t idx_buf[512];
for (int t = 0; t < count; ++t) {
    int b_val = b_row[k_ptr[t]];
    idx_buf[t] = (uint32_t)(a_ptr[t] - base) * 256u + b_val;
}

// 阶段2：SVE gather 查表 + 向量化累加
svfloat32_t acc = svdup_n_f32(0.0f);
int t = 0;
svbool_t pg = svwhilelt_b32(t, count);
while (svptest_any(svptrue_b32(), pg)) {
    svfloat32_t vals = svld1_gather_u32index_f32(pg, sub_float, idx_buf + t);
    acc = svadd_f32_m(pg, acc, vals);
    t += svcntw();
    pg = svwhilelt_b32(t, count);
}
#pragma omp atomic
C[i * S + j] += svaddv_f32(svptrue_b32(), acc);
```

关键变化（对比旧版 BF16 分阶段方案）：

| 维度 | 旧版（BF16） | 新版（float SVE gather） |
|------|------------|------------------------|
| 子表格式 | BF16 2B/entry，需构建 | float 4B/entry，直接从 LUT 切分 |
| 阶段1 工作 | BF16 查表 + 转 float | 纯索引计算（无访存表） |
| 阶段2 工作 | SVE 顺序 load + 累加 | SVE gather + 累加（一次完成） |
| 中间缓冲区 | `local_vals[]`存 float 结果 | `idx_buf[]`存 uint32_t 索引 |
| 数据流宽度 | 标量逐元素查表（~4 cyc/元素） | SVE gather 8 元素/指令 |

### 3.3 计时方式

```
t0              t1                      t2          barrier     t3
├────────────────┼──────────────────────┤           ├────────────┤
│  标量索引计算   │  SVE gather + 累加   │           │  等待最慢核  │
│  for t in cnt:  │  svld1_gather        │           │            │
│    B_T 收集     │  svadd_f32_m 循环     │           │            │
│    索引计算      │  svaddv_f32 归约     │           │            │
│                │  #pragma omp atomic  │           │            │
└────────────────┴──────────────────────┘           └────────────┘
  → 计算 = (t1 - t0) + (t2 - t1)          → sync = (t3 - t2)
```

| 指标 | 含义 |
|------|------|
| `compute(min~max)` | G 个核各自完成 (i,j) 循环的时间范围 |
| `sync` | barrier 等待时间（负载不均 + 调度抖动） |
| `total = max(compute) + sync` | wall-clock 总耗时 |
| `GOP/s = N × S × L / total_us / 1000` | 每秒十亿次查表操作 |

---

## 4. 实验结果

### 4.1 输出（100 次 warmup + **10000 次采样**，含 PMU cache 分析）

**BF16 单核基线（顺序索引，128 KiB L2）：**

| 方式 | 总耗时(μs) | GOP/s | 正确 |
|------|-----------|-------|------|
| BF16 标量 | 17074.8 | 1.26 | OK |
| BF16 SVE | 12334.3 | 1.74 | OK |
| SVE 加速比 | — | **1.38×** | — |

**float 单核基线（原表，256 KiB L2）：**

| 方式 | 总耗时(μs) | GOP/s | 正确 |
|------|-----------|-------|------|
| float 标量 | 15174.4 | 1.42 | OK |
| float SVE | 12478.2 | 1.72 | OK |
| SVE 加速比 | — | **1.22×** | — |

**BF16 分组查表（BF16 子表，标量索引 + SVE gather + atomic）：**

| 方案 | 线程 | 子表 | 级别 | 计算(核min~max) | 同步 | 预处理 | L1-miss% | L2-miss% | 总耗时mean±sd | 总耗时min | GOP/s | 正确 |
|------|------|------|------|----------------|------|-------|---------|---------|:---:|:---:|:---:|------|
| G=1 | 1 | 256 KiB | L2 | 19657.4~19657.4 | 0.0 | 465.2 | 27.54% | 0.01% | 20152.0±644.6 | 19657.4 | 1.09 | OK |
| G=2 | 2 | 128 KiB | L2 | 9192.6~9315.4 | 199.7 | 381.7 | 16.40% | 0.10% | 9638.5±185.4 | 9315.4 | 2.31 | OK |
| G=4 | 4 | **64 KiB** | **L1边界** | **4689.5~4725.6** | 387.0 | 353.1 | **4.14%** | 0.75% | **5093.5±385.3** | 4725.6 | **4.55** | OK |
| G=8 | 8 | **32 KiB** | **L1** | **2405.0~2713.7** | 411.8 | 366.9 | **1.27%** | 1.50% | **2973.9±129.4** | 2713.7 | **7.92** | OK |
| G=16 | 16 | 16 KiB | L1 | 1414.2~1780.0 | 617.4 | 392.6 | 7.28% | 1.01% | 2037.9±218.9 | 1780.0 | 12.08 | OK |
| G=32 | 32 | 8 KiB | L1 | 979.2~1626.4 | 822.3 | 419.4 | 8.08% | 1.40% | 1897.4±140.5 | **1626.4** | **13.22** | OK |
| G=64 | 64 | 4 KiB | L1 | 949.7~2262.7 | 1417.2 | 374.9 | 9.18% | 2.41% | 2447.4±58.2 | 2262.7 | 9.50 | OK |

**float SVE 分组查表（全精度子表，SVE gather + atomic）：**

| 方案 | 线程 | 子表 | 级别 | 计算(核min~max) | 同步 | 预处理 | L1-miss% | L2-miss% | 总耗时mean±sd | 总耗时min | GOP/s | 正确 |
|------|------|------|------|----------------|------|-------|---------|---------|:---:|:---:|:---:|------|
| F32-G=1 | 1 | 256 KiB | L2 | 19083.7~19083.7 | 0.0 | 407.1 | 27.49% | 0.02% | 20079.0±757.6 | 19083.7 | 1.13 | OK |
| F32-G=2 | 2 | 128 KiB | L2 | 9151.1~9314.0 | 282.4 | 308.0 | 16.61% | 0.10% | 9651.9±182.5 | 9314.0 | 2.31 | OK |
| F32-G=4 | 4 | 64 KiB | L1边界 | 4581.9~4738.6 | 377.7 | 302.0 | 3.98% | 0.58% | 5084.5±323.6 | 4738.6 | 4.54 | OK |
| F32-G=8 | 8 | 32 KiB | L1 | 2451.0~2690.2 | 409.0 | 358.2 | 1.35% | 1.53% | 2957.2±132.5 | 2690.2 | 7.99 | OK |
| F32-G=16 | 16 | 16 KiB | L1 | 1361.0~1770.6 | 500.9 | 361.4 | 7.23% | 0.94% | 1910.7±70.8 | 1770.6 | 12.14 | OK |
| F32-G=32 | 32 | 8 KiB | L1 | 963.6~1618.5 | 757.2 | 331.8 | 8.08% | 1.59% | 1893.2±141.3 | 1618.5 | 13.28 | OK |
| F32-G=64 | 64 | 4 KiB | L1 | 1222.1~2244.2 | 1339.4 | 335.6 | 9.39% | 2.45% | 2443.6±58.8 | 2244.2 | 9.58 | OK |

**float SVE 核数扫描（原表 256 KiB L2，行并行无分组）：**

| 方案 | 线程 | 子表 | 级别 | L1-miss% | L2-miss% | 总耗时mean±sd | 总耗时min | GOP/s | 正确 |
|------|------|------|------|---------|---------|:---:|:---:|:---:|------|
| floatSVE | 1 | 256 KiB | L2 | 54.75% | 0.036% | 13080.0±757.9 | 12186.0 | 1.76 | OK |
| floatSVE | 2 | 256 KiB | L2 | 54.42% | 0.039% | 6535.3±396.6 | 6053.9 | 3.55 | OK |
| floatSVE | 4 | 256 KiB | L2 | 54.45% | 0.070% | 3116.2±27.0 | 3045.2 | 7.06 | OK |
| floatSVE | 8 | 256 KiB | L2 | 51.27% | 0.097% | 1883.6±197.7 | 1540.2 | 13.96 | OK |
| floatSVE | 16 | 256 KiB | L2 | 51.92% | 0.181% | 973.8±161.3 | 767.0 | 28.02 | OK |
| floatSVE | 32 | 256 KiB | L2 | 43.10% | 0.312% | 788.2±288.2 | 392.1 | 54.82 | OK |
| floatSVE | 64 | 256 KiB | L2 | 34.13% | 0.595% | 692.1±30.8 | 571.2 | 37.63 | OK |
| floatSVE | **80** | 256 KiB | L2 | — | — | — | — | **30.71*** | OK |

> *80 核数据来自实验6旧版基线，新版未重测。

**列含义说明：**

| 列 | 含义 | 计算方式 |
|----|------|---------|
| **方案** | 分组方案标识：G=N 表示切为 N 个子表用 N 核并行；F32-G=N 为 float 子表版本 | — |
| **线程** | OpenMP 线程数，等于 G | — |
| **子表** | float 子表物理大小 | step × 256 × 4B |
| **级别** | 子表所在 cache 层级（L1/L2），由子表大小与 L1d 64 KiB 对比判定 | — |
| **计算(核min~max)** | G 个核各自完成计算 (i,j) 循环的耗时范围（μs） | 10000 次平均 |
| **同步** | barrier 等待时间（μs） | 10000 次平均 |
| **预处理** | 分组重排 + 子表构建的一次性耗时（μs） | — |
| **L1-miss%** | L1 数据 cache 缺失率 | L1D_CACHE_REFILL / L1D_CACHE × 100 |
| **L2-miss%** | L2 数据 cache 缺失率 | L2D_CACHE_REFILL / L2D_CACHE × 100 |
| **总耗时mean±sd** | wall-clock 总耗时平均值 ± 标准差（μs） | mean = max(compute) + sync |
| **总耗时min** | 10000 次测量中的最优（最小）值（μs） | — |
| **GOP/s** | 每秒十亿次查表操作 | N × S × L / total_us / 1000 |
| **正确** | 计算结果与标量基线对比验证 | — |

### 4.2 L1-miss% 验证 Cache 层级

PMU 数据直接证实了各 G 值（BF16 分组方案）的真实 cache 行为：

| G | 子表大小 | 理论 Cache | 实测 L1-miss% | 解读 |
|---|---------|-----------|--------------|------|
| 1 | 256 KiB | L2 | **27.54%** | 远超 L1d，每次查表大量 L2 访问 |
| 2 | 128 KiB | L2 | **16.40%** | 仍超出 L1d，但压力减半 |
| 4 | 64 KiB | L1 边界 | **4.14%** | 接近 L1 容量上限，仍有部分 L2 访问 |
| 8 | 32 KiB | L1 | **1.27%** | 完全 L1 驻留 |
| 16 | 16 KiB | L1 | **7.28%** | 子表虽小，16 核 MESI 一致性流量导致 miss 升高 |
| 32 | 8 KiB | L1 | **8.08%** | MESI 流量进一步增加 |
| 64 | 4 KiB | L1 | **9.18%** | 64 核高频 atomic 竞争引发大量 cache 行失效 |

趋势解读：
- G=1~2：子表在 L2，L1-miss 较高（27.54%→16.40%）
- G=4~8：子表进入 L1 后 miss 率骤降至 4.14%→1.27%
- G≥16：子表虽完全 L1 驻留，但核数增加导致 **MESI 一致性流量**成为主要 miss 源，miss 率从 1.27%（G=8）升至 7.28%~9.18%（G=16~64）

所有 G 值的 L2-miss% 均 < 2.5%，验证了全表在 L2 内常驻。

### 4.3 可视化

![实验6结果可视化](exp6_visualization.png)

---

## 5. 分析

### 5.1 L1 查表降低计算时间

**BF16 分组方案（标量索引 + SVE gather）：**

| 配置 | 计算(核min) | 相对 G=1 加速 | L1-miss% |
|------|-----------|-------------|---------|
| G=1（L2, 1核） | 19657 μs | 1× | 27.54% |
| G=2（L2, 2核） | 9193 μs | 2.1× | 16.40% |
| G=4（L1边界, 4核） | 4690 μs | 4.2× | 4.14% |
| G=8（L1, 8核） | 2405 μs | 8.2× | 1.27% |
| G=16（L1, 16核） | 1414 μs | 13.9× | 7.28% |
| G=32（L1, 32核） | 979 μs | 20.1× | 8.08% |
| G=64（L1, 64核） | 950 μs | 20.7× | 9.18% |

**float SVE 分组方案：**

| 配置 | 计算(核min) | 相对 G=1 加速 | L1-miss% |
|------|-----------|-------------|---------|
| F32-G=1（L2, 1核） | 19084 μs | 1× | 27.49% |
| F32-G=2（L2, 2核） | 9151 μs | 2.1× | 16.61% |
| F32-G=4（L1边界, 4核） | 4582 μs | 4.2× | 3.98% |
| F32-G=8（L1, 8核） | 2451 μs | 7.8× | 1.35% |
| F32-G=16（L1, 16核） | 1361 μs | 14.0× | 7.23% |
| F32-G=32（L1, 32核） | 964 μs | 19.8× | 8.08% |
| F32-G=64（L1, 64核） | 1222 μs | 15.6× | 9.39% |

关键发现：
- G=32 达到最佳计算时间（BF16: 979 μs, float: 964 μs），相对单核加速约 **20×**
- BF16 和 float 方案的计算性能几乎一致（两者计算量相同，子表大小相等时 cache 行为一致）
- 与旧版 BF16 chunked 方案（最优计算时间 5802 μs，G=64）相比，新版 SVE gather 方案计算时间降低 **5.9×**（979 vs 5802 μs）
- 计算加速比在 G=32 前几乎线性（核数加倍，计算时间减半），说明 SVE gather 有效利用了 L1 带宽
- G=64 时计算时间不再下降（BF16 版 950 μs vs G=32 的 979 μs），Atomic 竞争和 MESI 流量达到饱和

### 5.2 Atomic 同步开销

**BF16 分组方案：**

| G | 同步(μs) | 占总耗时比例 |
|---|---------|------------|
| 1 | 0.0 | **0%** |
| 2 | 199.7 | **2.1%** |
| 4 | 387.0 | **7.6%** |
| 8 | 411.8 | **13.8%** |
| 16 | 617.4 | **30.3%** |
| 32 | 822.3 | **43.3%** |
| 64 | 1417.2 | **57.9%** |

**float SVE 分组方案：**

| G | 同步(μs) | 占总耗时比例 |
|---|---------|------------|
| 1 | 0.0 | **0%** |
| 2 | 282.4 | **2.9%** |
| 4 | 377.7 | **7.4%** |
| 8 | 409.0 | **13.8%** |
| 16 | 500.9 | **26.2%** |
| 32 | 757.2 | **40.0%** |
| 64 | 1339.4 | **54.8%** |

Atomic 同步占比随 G 增大持续上升：G=32 时占总耗时 40~43%，G=64 时超过 50%。这意味着在 G=32 之后，**Atomic 竞争成为首要瓶颈**——增加核数不仅不加速，反而因更激烈的缓存行争用导致总耗时增加（G=64 总耗时 2447 μs vs G=32 的 1897 μs）。

### 5.3 负载均衡

**BF16 分组方案：**

| G | 每组平均元素数 | 计算 min~max | 不均衡度 |
|---|--------------|-------------|---------|
| 1 | 512 | 19657~19657 | 1.0× |
| 2 | 256 | 9193~9315 | 1.01× |
| 4 | 128 | 4690~4726 | 1.01× |
| 8 | 64 | 2405~2714 | 1.13× |
| 16 | 32 | 1414~1780 | **1.26×** |
| 32 | 16 | 979~1626 | **1.66×** |
| 64 | 8 | 950~2263 | **2.38×** |

**float SVE 分组方案：**

| G | 每组平均元素数 | 计算 min~max | 不均衡度 |
|---|--------------|-------------|---------|
| 1 | 512 | 19084~19084 | 1.0× |
| 2 | 256 | 9151~9314 | 1.02× |
| 4 | 128 | 4582~4739 | 1.03× |
| 8 | 64 | 2451~2690 | 1.10× |
| 16 | 32 | 1361~1771 | **1.30×** |
| 32 | 16 | 964~1619 | **1.68×** |
| 64 | 8 | 1222~2244 | **1.84×** |

G=16 起负载不均显著加剧（1.26~1.30×），G=64 时达到 1.84~2.38×。绝对值来看，G=64 时最慢核比最快核慢 1043~1313 μs，对应约 6~8 个 \((i,j)\) 行的额外时间。

负载不均的原因是随机 idxA 分布导致各组 `row_count[i*G+g]` 的波动随 G 增大而放大（二项分布方差 \(\sqrt{N \cdot \frac{1}{G} \cdot (1-\frac{1}{G})}\) 在 G 大时相对波动更大）。

### 5.4 对比基线

**行并行基线（float SVE 核数扫描，原表 256 KiB L2）：**

| 核数 | 总耗时min(μs) | GOP/s | L1-miss% | L2-miss% |
|------|:---:|:---:|:---:|:---:|
| 1 | 12186.0 | 1.76 | 54.75% | 0.036% |
| 2 | 6053.9 | 3.55 | 54.42% | 0.039% |
| 4 | 3045.2 | 7.06 | 54.45% | 0.070% |
| 8 | 1540.2 | 13.96 | 51.27% | 0.097% |
| 16 | 767.0 | 28.02 | 51.92% | 0.181% |
| 32 | 392.1 | 54.82 | 43.10% | 0.312% |
| 64 | 571.2 | 37.63 | 34.13% | 0.595% |
| 80* | ~700 | ~30.71 | — | — |

> *80 核数据来自旧版基线

**对比分析：**

| G | 分组方案 GOP/s | 同核数行并行 GOP/s | 分组相对行并行 |
|---|:---:|:---:|:---:|
| 1 | 1.09 | 1.76 | **0.62×** |
| 2 | 2.31 | 3.55 | **0.65×** |
| 4 | 4.55 | 7.06 | **0.64×** |
| 8 | 7.92 | 13.96 | **0.57×** |
| 16 | 12.08 | 28.02 | **0.43×** |
| 32 | 13.22 | 54.82 | **0.24×** |
| 64 | 9.50 | 37.63 | **0.25×** |

分组方案始终低于同核数行并行，且差距随核数增加而扩大。G=32 时，行并行（32 核）达 54.82 GOP/s，是分组方案（13.22 GOP/s）的 **4.1×**。

原因：
1. **行并行每个核写不同的 C[i][j] 行**，缓存行私有，无需 atomic
2. **行并行 SVE gather 直接查完整 LUT**，无索引计算开销
3. **atomic 竞争**是分组方案的核心瓶颈——G≥16 后同步占比超 26%
4. 行并行 L1-miss% 高达 ~50%，但 32 核的吞吐增益远超过 cache miss 惩罚

### 5.5 对比 x86

| 指标 | x86 i7-12700 (10核) | 鲲鹏 (80核) |
|------|-------------------|------------|
| L1d | 48 KiB | 64 KiB |
| 最优 G（分组方案） | G=8（8核） | G=32（32核） |
| 最优分组 GOP/s | 8.31 | 13.22 |
| G=8 同步占比 | 361 μs (14%) | 412 μs (13.8%) |
| 基线 vs 分组 | 10.94 vs 8.31（差 24%） | 30.71 vs 13.22（差 **57%**） |

鲲鹏分组方案经 SVE gather 优化后，最优 GOP/s 从旧版 2.59 提升至 13.22（**5.1×**），与基线差距从 92% 缩小到 57%。优化主因是 SVE gather 替代了旧版 BF16 chunked 标量查表，大幅降低了标量相位的时间。

### 5.6 汇编指令分析

基于 `lookup_sve_grouped_omp` 函数的反汇编（`g++ -O3 -march=armv8.2-a+sve`），统计指令构成，分析瓶颈性质：

**按指令名称统计：**

| 指令 | 数量 | 占比 |
|------|:---:|:---:|
| `add` | 32 | 14.3% |
| `ldr` | 29 | 12.9% |
| `b` | 20 | 8.9% |
| `mov` | 18 | 8.0% |
| `str` | 13 | 5.8% |
| `cmp` | 12 | 5.4% |
| `sub` | 10 | 4.5% |
| `ldp` / `stp` | 16 | 7.1% |
| `fmov` | 7 | 3.1% |
| `nop` | 5 | 2.2% |
| `bl`（函数调用） | 4 | 1.8% |
| SVE 指令（`ld1w`/`ld1b`/`st1w`/`ptrue`/`whilelt`/`whilelo`/`ptest`/`cntw`/`cntb`/`fadd`/`faddv`/`addvl`/`decb`/`decw`） | 22 | 9.8% |
| 其他标量（`lsl`/`sbfiz`/`sxtw`/`mul`/`smull`/`and`/`cset`/`ccmp`/`csel`/`movk`） | 15 | 6.7% |
| `casal`（原子操作） | 1 | 0.4% |
| `ret` | 1 | 0.4% |
| **合计** | **224** | **100%** |

**按功能模块分类（估算）：**

| 功能模块 | 估计指令数 | 占比 | 说明 |
|---------|:--------:|:---:|------|
| 函数框架（prologue/epilogue/栈） | ~35 | 15.6% | `stp`/`ldp`/`mov`/`addvl`/`cntb`/`decb`/`nop`/`ret` |
| i/j 外层循环控制（N=128×S=328） | ~45 | 20.1% | `cmp`/`b`/`cbz`/`cbnz`/`cset`/`ccmp` + 循环计数器 `add`/`sub` |
| 阶段1：标量索引计算（内层标量循环） | ~60 | 26.8% | `ldrb` a_ptr + `sub`/−base + `lsl`/`sbfiz`×256 + `ldr`/`ldrsw` k_ptr + `ldrb` b_row + `add` 组合 + `str` idx_buf |
| 阶段2：SVE gather + 累加（内层 SVE 循环） | ~22 | 9.8% | `whilelt`/`whilelo`/`ptrue`/`ptest` 谓词 + `ld1w` gather + `fadd` + 地址 `add` + 分支 `b` |
| 归约 + Atomic | ~6 | 2.7% | `faddv` + `casal`+分支 CAS 重试 |
| C 矩阵地址计算 + 子表基地址 | ~56 | 25.0% | `mul`/`smull` i×S + `ldp`/`ldr`/`add` 加载成员变量 + 偏移计算 |

**关键解读：**

1. **标量指令统治（~88%）**：虽然使用 SVE gather 查表，但函数框架、循环控制、索引计算、地址计算等标量部分占总指令的绝大多数。SVE 向量指令仅 ~22 条（9.8%）。
2. **阶段1（标量索引计算）是最大单一模块（~27%）**：对每个元素执行 `a_ptr[t]-base`、`×256`、`k_ptr[t]` 间接寻址、`b_row` 加载、组合、存储——共 ~60 条指令，是函数体中最重的部分。
3. **SVE gather 本身仅 1 条指令**：`ld1w`（带索引转换）一次性加载 8 个 float，但周围配套的谓词管理（`whilelt`/`whilelo`/`ptrue`/`ptest`/`cntw`）和循环控制额外增加了 ~10 条指令。
4. **Atomic 开销极小（< 3%）**：`casal` + 重试分支仅 ~6 条指令，因为 32 核竞争的主要开销在**缓存行 bouncing 的可变延迟**，而非指令数量。
5. **224 条指令 / 80 个输出元素** = ~2.8 条指令/（i,j），但每个(i,j)内部处理 count 个元素，实际热点在内部标量和 SVE 循环。

---

## 6. 结论

### 6.1 核心发现

1. **SVE gather + L1 子表显著提升计算性能**：G=32 时，BF16 分组方案计算时间降至 979 μs（相对单核 G=1 加速 20.1×），最优 GOP/s 达 **13.22**，相比旧版 BF16 chunked 方案（2.59 GOP/s）提升 **5.1×**

2. **SVE gather 直接查表消除中间缓冲区开销**：新版使用 `svld1_gather_u32index_f32` 直接从 float 子表加载数据到向量寄存器，取代了旧版"标量 BF16 查表 → 转 float → 写 local_vals → SVE load"的 4 步流程，计算时间降低 ~6×

3. **BF16 与 float 子表性能几乎一致**：在子表大小相等时（float 子表为 BF16 的两倍），两者的计算时间和 cache miss 率基本相同——说明 cache miss 取决于子表物理大小，与数据类型无关

4. **Atomic 竞争仍是主要瓶颈**：G≥16 后同步占比超过 26%，G=64 时超过 50%。G=64 的 wall-clock 总耗时（2447 μs）高于 G=32（1897 μs），说明过大的 G 值因 atomic 竞争和 MESI 流量反而降低整体性能

5. **G=32 是最优折中点**：13.22 GOP/s（BF16）/ 13.28 GOP/s（float），Atomic 与 L1 收益的最佳平衡

6. **分组方案仍无法超越行并行基线**：32 核行并行达 54.82 GOP/s，是最优分组方案的 4.1×；行并行无 atomic 竞争的优势在核数增加时更加明显

### 6.2 根本原因

```
查表法矩阵乘法的瓶颈变化（新版 SVE gather）：
  单核（L2 查表）：  L2 随机 gather 延迟                     (~50% L1-miss)
  分组查表（新版）：  L1 gather + SVE 向量化                (L1-miss 1~9%)
                    + atomic 缓存行竞争                     (~50-200 cyc/atomic)
                    + 负载不均                             (G=32 时 1.66×)
                    + 受限并行度（G 核 vs 80 核）
  → SVE gather 大幅降低了查表成本，但 atomic 竞争仍未解决

  旧版瓶颈（BF16 chunked, G=64）：
    标量相位 1975 μs    ← 新版通过 SVE gather 几乎消除
    SVE相位  5350 μs    ← Atomic + 归约
    同步     1186 μs    ← 负载不均 + barrier

  新版瓶颈（SVE gather, G=32）：
    计算     979~1626 μs ← 索引 + gather，大幅降低
    同步     822 μs     ← Atomic 竞争成为首要瓶颈
    预处理   419 μs     ← 一次性开销
```

### 6.3 更可行的优化方向

| 方向 | 预期收益 | 说明 |
|------|---------|------|
| 两级查表 | ~2-4× | 粗表 + 细表，可实现**无 atomic 的 L1 驻留查表**，保留 80 核行并行 |
| INT8 I8MM 硬件指令 | ~10-100× | 计算密集型，可充分发挥 320核 SVE 算力 |

---

## 7. 代码索引

| 函数 | 行号 | 说明 |
|------|------|------|
| `float_to_bf16()` | 273 | float32→BF16 转换（取高 16 位，仅用于 BF16 基线对比） |
| `GroupData` | 280 | 分组数据结构体（行计数 + 偏移） |
| `preprocess_groups()` | 289 | 按 idxA 对每行分组重排 |
| `build_subtables_bf16()` | 321 | 从完整 LUT 切分 BF16 子表（旧版） |
| `build_subtables_bf16_float()` | 335 | BF16 子表直接以 float 格式存储（无转换） |
| `build_subtables_float()` | 355 | 从完整 LUT 切分 float 子表（新版，直接切片） |
| `lookup_sve_grouped_omp()` | 368 | 标量索引计算 + SVE gather 查表 + atomic 累加 |
| `exp6_l1_grouped_lut()` | 429 | 实验 6 主函数：G 值扫描 + BF16/float 双方案 + 基线对比 |

### 编译运行

```bash
# 运行实验
g++ -O3 -fopenmp -march=armv8.2-a+sve -o fp8_server \
    matmul_fp8_server.cpp -std=c++17 -lnuma

numactl --cpunodebind=0 --membind=0 \
  OMP_NUM_THREADS=80 OMP_PLACES=cores OMP_PROC_BIND=close \
  ./fp8_server

# 反汇编分析
g++ -O3 -c -march=armv8.2-a+sve -fopenmp matmul_fp8_server.cpp \
    -std=c++17 -o /tmp/fp8.o
objdump -d /tmp/fp8.o > /tmp/fp8_disasm.s
```
